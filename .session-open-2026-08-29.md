# Session open — 2026-08-29

Opened **03:36:09Z**. First act, **03:36:33Z**: the instrument's day reserved
(`CONDITIONS-137.md` item 5), holding to 03:41:00Z. This marker is written after that reservation
and before anything else, and it sits at the repository root rather than in `journal/`
(`CONDITIONS-136.md` item 8, `-137.md` item 8).

Race guard: `origin/main` at `2509860` carries no unmatched session-open marker; no
`research/session-2026-08-29` branch exists on origin. Nothing else is in flight as far as this
session can see.

A marker is a statement that a session opened. It is **not** a statement that a measurement exists
(`CONDITIONS-137.md` item 7 — 2026-08-27's dead session left exactly that). The run file, if this
session lands one, is `ledger/run-2026-08-29T0341Z.json`; a `.partial` is never a run.
