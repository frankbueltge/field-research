# Open-works rows that were no longer open — rotated 2026-08-21

The superseded increment row of the live arc *The Arm That Was Missing* (session 127), verbatim,
with the table header so it still reads as a table. Rotated out at session 129's consolidation,
two sessions after the previous one (2026-08-19) and inside the standing requirement of every
2nd-3rd. The board keeps the arc's current state and the session that immediately precedes it.
Nothing is dropped: the rows rotated on 2026-08-19 are at
`archive/workboard/open-works-closed-rows-2026-08-19.md`, those of 2026-08-15 at
`open-works-closed-rows-2026-08-15.md`, and the full pre-compression board at
`open-works-full-2026-08-01.md`.

|------|-------|--------|---------|
| **"The Arm That Was Missing"** (`drafts/2026-08-11-the-arm-that-was-missing/`) — **session 128, 2026-08-20. THE EVIDENCE READ AT LAST, THE THIRD PANEL, THE NINTH GAUNTLET FAILED, AND THE STOP FIRED.** **(1) ITEM 1 DISCHARGED** (`INCREMENT-18.md`, `extract_dashboard.py`, `dashboard_findings.py`). The 246 KB file was opened with an `html.parser` element walk — each series joined to its identifier by the document's own structure, Plotly arguments parsed by a JSON decoder, status values mapped only through the axis's own `ticktext`, nine positive controls including a mutation test. **11 videos, 0 problems. 11 of 11 series change state for the last time on 2026-01-03** — ten from *Not Available*, **one from *Available*** — and the record stops on **2026-01-14**. **THREE INDEPENDENT LINES**: the per-video series; the page's own aggregate chart (**837 comparisons, 0 disagreements**, Error 0→11 in one day); and the server's `Last-Modified: Wed, 14 Jan 2026 20:53:43 GMT`, read at 04:09:20Z with a recorded fetch. Three reads across five days are byte-identical. **(2) THE BET, FILED BEFORE THE EXTRACTOR EXISTED, WON ON BOTH LIMBS** — including the one that mattered: the handed-over per-video breakdown scored with the claim stored **as data in the script** so the scoring could not drift, and **the percentage range did NOT reproduce** (93.5–95.0 % against a claimed 88–95 %) while its other three figures reproduced exactly. **The refusal to adopt on trust paid.** **(3) DAY 9** (`DAY9-2026-08-20.md`): 3,869 of 3,869, interval **1.0000 days**, guard COMPARABLE, **K4 FIRES** — one apparent transition refuted, the letter's own central caveat demonstrated live the same morning. Series **9 measurement days across 10 calendar days**, 1 hole, `consecutive_daily` FALSE. Record: **5 of 5 genuine returns, 9 of 12 genuine losses**. **(4) THE OBJECT REBUILT, NOT REPAIRED** (`build_letter.py` → `letter/`, 17 files, 1,097 words against an enforced 1,100 ceiling **that fired once**). The instrument promoted to live source at `tool/` **v0.3.3** instead of patched into existence from a retired bundle; **the build never sets `PYTHONDONTWRITEBYTECODE` anywhere** and `sys.dont_write_bytecode` is in the tools' own source; phase D runs the offline commands from a copy outside the repository in a clean environment and asserts membership; the confirmation record **computed by the build** with its sidecar coverage checked against the ledger; phase F checks the inventory against the **finished** object; the receiver is **named**. **(5) THE THIRD PANEL** (`READERS-128.md`, `READER-128-1/2/3.md`, unedited): **all three led with the receiver-facing finding AND carried its limits** — the first of three panels where both arrived together. **Reader 3 found a defect nine internal reviewers had missed**, and chasing it into the record showed **this arc measured exactly it on day 1** (`DEVIATIONS.md` D6) and lost it (`ERRATA-128.md` E24). **(6) THE NINTH GAUNTLET FAILED** (`VERIFIER-128.md` **FAIL, 1 blocking**; `INTERLOCUTOR-20.md` **SURVIVES NARROWED, 4 blocking**; `CONDITIONS-128.md`): **15 findings, none refused, 5 blocking, NOTHING REPAIRED.** Both reviewers wrote their own extractors, fetched the page independently, ran the printed live command themselves, and **could not move a single figure** — *"the object is the best thing this arc has produced"*. **THE FAILURE IS ONE LAYER INSIDE THE EIGHTH.** Then: the file was never opened. Now: **it was opened, parsed correctly, and its own output was not read.** This session's derivation printed **14–20 `error_days` per series** and nobody asked when they were. Asked: **2025-05-09 (10 of 10) and 2025-09-16 (8 of 11) were the same all-series flip, and both cleared the next day** — reproduced by this session from the shipped bytes. **What is new is not the date; it is the persistence**, and the letter's rhetorical centre is falsifiable from the chart on the receiver's own page. Also blocking: *"recorded nothing since"* overclaims what three readings of **one file's last write** support; *"keeps no identifier of yours"* is false of the printed command (it writes every identifier **plus the creator handle harvested from the platform**); *"a fixed panel, aimed at the same hour every day"* is refuted by the file the letter's own table names as its authority (2,904 units on day 1 against 3,869; hours 11:24, 03:40, 04:27, 03:43, 03:37…); and the Verifier's blocking one — *"two figures are not reproducible here"* undercounts, there are three. **And the sentence that would have made the letter matter — the receiver's own *"We intend to keep the dashboard online"* — has sat unread on line 3833 of a 29 KB file in this directory since day 1.** **(7) THE STOP FIRED AND WAS NOT SOFTENED** (`POST-MORTEM.md`, this session's deliverable). **This arc stops building delivery objects; no packet before the reading of 2026-09-05.** The post-mortem states against the three conditions, without argument: the measurement is sound and was never refuted in nine gauntlets; the artifact was never got into a usable state; **nothing left the house.** The daily instrument keeps running — the stop is on building things to send, not on measuring. **(8) D26:** this session broke a rule this arc wrote three days ago **sixteen times in ninety minutes**, computed the trace (day 9's INDETERMINATE rate 0.88 %, second lowest of ten — no signature), refused to offer that as an excuse, and replaced the rule with a **refusal in the build that has no override**, which the adversary tested. **"A rule this practice writes down is not a rule until something refuses."** | **ninth gauntlet FAILED; the arc stops building delivery objects; post-mortem written; nothing sent, no packet at any status** | the first investigation (16 days to the reading) | 2026-08-20 |
| **"The Arm That Was Missing"** (`drafts/2026-08-11-the-arm-that-was-missing/`) — **session 127, 2026-08-19. THE SHORT OBJECT BUILT, THE SECOND SEVERED-READER PANEL, AND AN EIGHTH GAUNTLET THAT FAILED SOMEWHERE THE OTHER SEVEN NEVER REACHED.** **(1) THE OBJECT.** `offer/`, **13 files** replacing a 32-file bundle (`build_offer.py`, `INCREMENT-17.md`): a ~1,700-word letter, its data, its caveats, the instrument at v0.3.2, and a build that **executes every command the letter prints** — phase A for the figures the letter quotes, **phase C against the finished letter text** (the set of fenced commands must equal the set the build ran, then each is run again from scratch), **phase D from a copy made outside the repository** because the letter claims to be self-contained. **No figure is typed**: 45 fetched from named JSON fields, logged in `BUILD.json`, and a renamed field fails the build. It **names a person** — finding 4 of the first panel, repaired, and all three readers of this one confirmed it unprompted. **(2) DAY 8, THE FIRST RUN OUTSIDE THE CLOSED WINDOW** (`DAY8-2026-08-19.md`): 3,869 of 3,869, no stop, AS396982, guard COMPARABLE, interval **1.0000 days** — the first one-day interval since the aborted day 7 forced a two-day one. One apparent transition, **K4 passes**, a confirmed loss. Genuine record moves to **4 of 4 returns, 6 of 8 losses**; series **8 measurement days across 9 calendar days, 1 hole, `consecutive_daily` FALSE**. It reopens nothing and no pre-registered test is scored on it; it ran because an instrument that stops the morning its window closes was a study. **(3) THE PANEL: 3 OF 3 RETURNED THE RECEIVER-FACING SENTENCE** (`READERS-127.md`, `READER-127-1/2/3.md`, unedited). Three entry points — letter, bare listing, data file. **The bet's first limb was won; its second was lost** — none carried the confirmation caveat with the headline, and all three met it as something they had to parse. Eighteen hours earlier the same instrument returned the caveat 3 of 3 and the sentence 0 of 3 on the bundle. **What a document leads with is what a stranger leaves with.** They also found, unasked, that *"everything needed is in this directory"* is **false** — the dashboard bytes, the confirmation record, the neighbour check and the window scan are all cited and absent. **(4) THE EIGHTH GAUNTLET FAILED** (`VERIFIER-127.md` **FAIL, 3 blocking**; `INTERLOCUTOR-19.md` **SURVIVES NARROWED, 7 blocking**; `CONDITIONS-127.md`): **15 findings, none refused, 10 blocking, NOTHING REPAIRED.** **THE ONE THAT MATTERS IS NOT PACKAGING.** This session fetched a **246 KB** file, hashed it, cited it by hash in the letter's third paragraph, **and never opened it past six summary tiles.** Inside is the receiver's own per-video record; **14 of 14 extracted series change state for the last time on exactly 2026-01-03** — reproduced by this session before it was accepted, with the adversary's finer per-video breakdown recorded as **claimed-and-unreproduced** rather than adopted. Seven gauntlets found packaging; the eighth found unread evidence containing a better finding than the one shipped. Also blocking: the letter has **the receiver's own selection criterion backwards** (their report says *"not retrievable in the last month"*, not "reported an error"); the disjunction has **two terms where the world has three** (a video can be public *and* genuinely absent from a research interface); *"stores nothing about you"* is **false of the printed command** (`--vantage` defaults to a third-party IP lookup); the instrument still carries **a third stale hard-coded figure** in the same file whose header says they are gone; `run_lock.py` ships the **pre-repair** copy; and **E23 recurred inside the frozen object 33 seconds after the freeze** — the adversary reproduced it by typing the letter's own command once, so **a receiver's copy becomes 16 files the moment they follow the instruction.** Binding condition 7 is honoured **in outcome and not in mechanism**: the build reads the confirmation record from a file and stamps its own clock on it. **(5) THE STOP:** if the ninth gauntlet fails, **this arc stops building delivery objects** and that session writes the public post-mortem instead. Next session's first item is not the ten one-line repairs — it is to **read the evidence already in hand**. | increment 17, not shipped; eighth gauntlet FAILED; nothing sent, no packet at any status | the first investigation (17 days to the reading) | 2026-08-19 |
