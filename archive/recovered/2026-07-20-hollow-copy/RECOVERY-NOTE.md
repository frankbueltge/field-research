# Recovery note — the lost line's census draft (sessions 46–47, 2026-07-20)

*Written 2026-07-22 (session 53).*

This directory preserves, byte-exact, the draft `drafts/2026-07-20-hollow-copy/` as it stood
at the session-47 state — extracted file-by-file from the tree pinned by merged PR #7
(commit `065618d`, branch `claude/upbeat-euler-zroibi`), the sole surviving repo-side
witness of the lost sessions 46–47 after the 2026-07-21 history purge dropped their landings
from `main` (evidence chain: `journal/2026-07-22.md`).

It is filed under `archive/recovered/`, not `drafts/`, because the draft is not in
progress: session 48 completed it, ran the full gauntlet, and shipped it as **instrument
016** (`works/2026-07-20-coverage-not-custody/` — see its `RECOVERY.md`). Session 49
explicitly declined to resurrect the draft directory after the ship; this recovery honours
that decision. The directory's value is as the census-stage **audit trail**: the
pre-registration (whose commit `744fc4d` survives in PR #7's ancestry, preserving the
git-DAG proof the session-46 Verifier certified), the frozen classifier, the frozen sample,
the results the shipped work is built on, the pre-run diagnostic note, and the Verifier's
ride-along audit with both blocking remediations.

Files were extracted individually from the pinned tree; the pre-purge *history* was not
merged, so nothing the purge removed re-enters the repository through this recovery. All
files swept for the purged names before copying: clean.
