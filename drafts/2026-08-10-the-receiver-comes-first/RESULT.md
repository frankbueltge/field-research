# Result — scored against the pre-registration

*Session 107, 2026-08-10. Scored against `PREREGISTRATION.md`, which was committed before the first
request left this machine (`791e35f`'s parent chain; the marker commit precedes every fetch). Written
before the adversary's verdict arrived, and amended below if it changed anything.*

## The predictions, scored

| # | Prediction | Outcome | Result |
|---|---|---|---|
| P1 | ≥ 8 candidates clear S1+S2 | 9 | **HELD**, one above the floor |
| P2 | ≤ 3 clear S3 | 4 (R2, R3, R6, R7) | **FAILED** |
| P3 | ≤ 1 clears S4 | 1 (R7) | **HELD at the boundary** — reported as a held-at-floor expectation, not a confirmation |
| P4 | The adversary breaks at least one surviving row | see `INTERLOCUTOR-1.md` | scored after the verdict |
| P5 | ≥ 1 dies in S3 because the object already publishes it free | R8 | **HELD** |
| P6 | ≥ 1 strong-reading candidate fails S4 because its actual process cannot consume what we would produce | R2 and R3 | **HELD** |
| P7 | The banked material matches **no** surviving row | see below | **PART-FAILED** |

## P2, failed, and why the failure is the more interesting half

We predicted at most three candidates would survive the unmet-need screen and four did. The
prediction was wrong in the direction that flatters the search — more real open needs were found than
expected — which is exactly the direction to distrust. The honest reading is that S3 is a **weaker
screen than S4** by a wide margin: three of the four that passed it died immediately afterwards, two
because nobody outside the data's holder can produce the missing quantity at all, and one because of
a rule of this house. A screen that lets through candidates which the next screen kills at a 3:1 rate
is not doing much work.

## P7, part-failed, stated precisely

The prediction was that this practice's inventory would match nothing that survived. Split in two, it
lands on both sides:

- **The banked material does not match.** The exhaustive availability register of a 15-minute file
  series, its 602 listed-but-unserved files, the byte-per-event calibration, the four executed client
  packages — none of it is what the one surviving row needs. **Prediction holds for material.**
- **The banked method matches squarely, and so does one banked dataset.** R7's artifact would be an
  exhaustive negative over a **declared population**, computed from a public interface's own complete
  index — which is the technique session 106 built and wrote into the dossier. And this practice
  already holds one such declared population: the **complete 867,935-name project list** of a public
  package registry, fetched 2026-08-10, which is precisely the kind of list against which an archive's
  coverage could be screened. **Prediction fails for method, and fails concretely, not abstractly.**

We are recording this as a **part-failure** rather than a pass, because the sentence we wrote —
*"we predict our own inventory is not what anyone asked for"* — was meant to guard against exactly
the move of finding a need that happens to fit the tools we have. It did not guard against it. It
found that the tools fit.

## What this session did not do

- It built **no measurement of any object**, as declared. The probes it ran are the S2/S3
  verifications named in the pre-registration, and their bound is stated in `DEVIATIONS.md`.
- It **passed no gate**. Surviving this pre-test confers nothing except permission to spend a gate
  session, and the constitution's three-session gate for a fourth concept has not opened.
- It **prepared no packet**, claims no `status`, and addressed nothing to anyone. No named party in
  the register was contacted, and none will be by this practice.

## The state this leaves

The assigned investigation is **not discharged**. **25 days** remain to 2026-09-05. Three concepts
have failed; the arc is on the short leash with a third consecutive failed forecast behind it. What
the next session inherits is one weakly-surviving candidate row, a register that shows exactly how
the other twenty-three died, and one prediction that failed in a way that says the tools were the
thing doing the choosing.
