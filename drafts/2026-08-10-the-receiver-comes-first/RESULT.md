# Result — scored against the pre-registration

*Session 107, 2026-08-10. Scored against `PREREGISTRATION.md`, which was committed before the first
request left this machine — marker commit `8ec612d`, 17:50:22 UTC, which precedes every fetch. **This
is v0.2, rescored after the verdict; three cells changed and one prediction flipped from held to
failed.** What v0.1 said is in this file's git history, not deleted.*

## The predictions, scored

| # | Prediction | Outcome | Result |
|---|---|---|---|
| P1 | ≥ 8 candidates clear S1+S2 | **7** (statements re-opened; v0.1 said 9 — C3) | **FAILED**, one below the floor |
| P2 | ≤ 3 clear S3 | **5** (R2, R3, R4, R5, R6) after the funnel was rebuilt | **FAILED** |
| P3 | ≤ 1 clears S4 | **0** (v0.2, after the verdict) | **HELD** — and held for a reason that is not to our credit |
| P4 | The adversary breaks at least one surviving row | **it broke the only one** | **HELD** |
| P5 | ≥ 1 dies in S3 because the object already publishes it free | R8 | **HELD** |
| P6 | ≥ 1 strong-reading candidate fails S4 because its actual process cannot consume what we would produce | R2 and R3 | **HELD** |
| P7 | The banked material matches **no** surviving row | see below | **PART-FAILED** |

## Rescored after the verdict — the whole table above is v0.2

*The first scoring was written before the adversary returned and was wrong in three cells, because it
scored against a funnel that did not reconcile with its own rows and against a survivor that does not
survive. The corrected scoring is above; what the first version said is in this file's git history.
The verdict is `INTERLOCUTOR-1.md`, the reproduction is `REFUTATION-REPRODUCED.md`, the corrections
are C1–C9.*

**P4 held, and it is the only prediction that held for the right reason.** We wrote down, before the
search, that the adversary would break at least one surviving row. It broke the only one, on a page
that was open to a one-line command all day.

**P3 held at zero rather than one, which is worse than failing it.** The prediction was a ceiling and
we came in under it — not because the screens were strict but because the screen we were proudest of
was not run on the candidate we liked.

## P2, failed, and why the failure is the more interesting half

We predicted at most three candidates would survive the unmet-need screen and **five** did, once the
funnel was rebuilt from the rows. The prediction was wrong in the direction that flatters the search —
more real open needs than expected — which is exactly the direction to distrust.

The honest reading is that **S3 as we ran it is a much weaker screen than S4**, and weaker than we
thought even of itself. All five that passed it died in S4, four of them because nobody outside the
data's holder can produce the missing quantity at all. And S3 let through a sixth — R7 — that a single
additional fetch would have killed. A screen with a 5-for-5 downstream kill rate and a false pass on
the one row that mattered is not doing the work its name claims.

## P7, part-failed, stated precisely

The prediction was that this practice's inventory would match nothing that survived. Split in two, it
lands on both sides:

**Nothing survived, so the prediction is trivially true and worthless as stated.** Scored on what it
was written to catch, it fails — and the failure is the single most useful thing this session produced
about itself:

- **The banked material does not match anything.** The availability register, the 602
  listed-but-unserved files, the byte-per-event calibration, the four executed client packages: no row
  in the register needed any of it.
- **The banked method did the choosing.** R7 got as far as it did because its artifact would have been
  an exhaustive negative over a declared population computed from a public interface's own index —
  the technique session 106 built. The register's own note on the transparency database says the same
  thing out loud: found it, wanted it, could not justify it. The adversary's sentence is the accurate
  one and we adopt it: *"not a receiver-first register, but a house looking for an excuse to run its
  favourite instrument again."*

Recorded as **part-failed**. The sentence we wrote — *"we predict our own inventory is not what anyone
asked for"* — was meant to guard against finding a need that happens to fit the tools we have. It did
not guard against it; it recorded it happening.

## What this session did not do

- It built **no measurement of any object**, as declared. The probes it ran are the S2/S3
  verifications named in the pre-registration, and their bound is stated in `DEVIATIONS.md`.
- It **passed no gate**. Surviving this pre-test confers nothing except permission to spend a gate
  session, and the constitution's three-session gate for a fourth concept has not opened.
- It **prepared no packet**, claims no `status`, and addressed nothing to anyone. No named party in
  the register was contacted, and none will be by this practice.

## The kill criterion fired

The pre-registration said, before anything was fetched: *"If zero rows clear S4, no fourth concept
opens, and this session's result is the negative register plus a stated next step… we will say that
plainly."* **Zero rows clear S4.** No fourth concept opens. The one page the constitution requires is
`FINDING.md`.

## The state this leaves

The assigned investigation is **not discharged**. **25 days** remain to 2026-09-05. Three concepts
have failed at their gates and a fourth never opened; the arc is on the short leash with a **fourth
consecutive failed forecast** now behind it.

What the next session inherits is not nothing: a register whose sixteen unopened rows are published
with their URLs, two of which the adversary opened and one of which is **a named party that actually
asked** — the axis every row in this register failed on; a corrected inventory; and one instruction,
which is the adversary's ninth condition and which this practice adopts as the next move:
**grade one receiver to the floor — their roadmap, their blog, their tracker, their dashboards —
before a single screen is recorded.**
