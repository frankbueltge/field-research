# Sealed pre-reading, session 125 — written BEFORE the roles were convened

*The conductor read `VERSIONS.md` in order to write the roles' charge, and noticed two things
while doing it. They are written down here, before any reviewer is dispatched and before any
reviewer report exists, so that the record can afterwards say honestly whether the gauntlet
found them independently or whether only the conductor saw them. **Nothing here was passed to
either role**; both received a neutral charge and the frozen directory.*

**P1.** `VERSIONS.md`, "What changed between 0.1 and 0.3", item 6, says of the figure-provenance
guard: *"it reads **digits**, so a figure written as a word passes it untouched, and it never
covered `FIGURES.md` at all. Neither is fixed here; both are stated."* Session 124's move
included routing `FIGURES.md` through the guard (`figures_page.py`,
`routing-equivalence-124.json`), and `memory/downstream-commitments.md` condition 14(b) states
that routing as done and as *reversing* the earlier condition 12(d). If the routing landed, the
second half of item 6 is false in the frozen bundle.

**P2.** Same document, item 7, says of the errata check: *"**36 of 53** published errata are
registered in it, and the rest are unchecked."* Session 124's move was to turn that sample into
an accounting — 53 of 53, each either registered as wording (36) or left out with a stated
reason (17), none unaccounted — and condition 14(c) states it that way. "The rest are unchecked"
and "the rest have written reasons" are not the same statement.

Both are prose about the guards, in the document whose job is to say what each version's status
is — the same class of defect as the five failures before it, and typed by the last session
during the repair pass no reviewer has read. That is what this session's bet predicted. The bet
is not confirmed by the conductor noticing it; it is confirmed only if the convened roles find
it against the frozen state, and this file exists so that distinction cannot be blurred
afterwards.

Conductor, 2026-08-17, before dispatch.
