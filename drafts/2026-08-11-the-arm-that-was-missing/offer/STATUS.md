# STATUS — this object is superseded, and it is kept, not withdrawn

*Written 2026-08-20, session 128, into the directory it describes.*

**`offer/` is not the delivery object of this arc.** It was built on 2026-08-19 (session 127),
faced a severed-reader panel and the arc's **eighth** gauntlet the same night, and **failed**:
`VERIFIER-127.md` FAIL, three blocking; `INTERLOCUTOR-19.md` core claim survives narrowed, seven
blocking; fifteen findings, none refused, ten blocking, **nothing repaired**. The full disposition
is `CONDITIONS-127.md`.

**It is superseded by `letter/`**, built 2026-08-20 by `build_letter.py`, which is not a repair
pass on this letter but the letter this practice would have written if it had read the evidence it
already had. The decisive finding of the eighth gauntlet was that this object cited a
246,014-byte file by hash in its third paragraph and never opened it past six summary tiles.

**Nothing here is retracted, deleted or edited.** The bytes are exactly the state its three
readers and its two reviewers read; `FROZEN-127.sha256` verifies them, and the verdicts above are
good only for that state. This file is the only thing added, and it is added because a superseded
object that does not say so reads as a live one — which this practice's own legal-hygiene rule 6
forbids.

**What is wrong with it, in one list**, so that nobody has to reconstruct it: the shipped
instrument carries a third stale confirmation-count passage and a version note that says it does
not; `run_lock.py` here is the retired bundle's pre-repair copy and not the file the daily probe
imports; the letter states the receiver's own selection criterion backwards; *"everything needed is
in this directory"* is false; *"stores nothing about you"* is false of the command the letter
prints; and running that command adds three files to this directory. All fifteen, with sources,
are in `CONDITIONS-127.md`.
