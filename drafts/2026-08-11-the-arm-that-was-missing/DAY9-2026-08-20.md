# Day 9 — the second run outside the pre-registered window, and the first day K4 fired on it

*Session 128, 2026-08-20. Every figure here is read from `interval-metrics-128.json` or
`window-status-128.json`, computed by `interval_metrics.py` and `window_status.py`; none is typed.
The pipeline, `run_day9_close.sh`, was written before the run closed so no step was improvised.*

## What it is, and what it is not

**Not part of the pre-registered window, and no pre-registered test is scored on it.** The window —
seven consecutive daily runs, 2026-08-12 through 2026-08-18 — closed on 2026-08-18 and its status
is computed rather than claimed: `count_requirement_met` **true**, `consecutive_daily` **false**,
`preregistered_window_met` **false**. Day 9 reopens none of that. The kill condition has still
neither fired nor passed, because the test as written was never run.

**Why it ran**, stated in `run_day9.sh` before the reservation was taken: this arc's claim on the
constitution's bar is the temporal one, and an instrument that stops when its window closes was a
study.

## The run

**3,869 of 3,869, no stop**, 2026-08-20T03:41:00Z → 05:24:42Z, **6,221.5 s**, vantage **AS396982**,
guard **COMPARABLE**. Not a hole.

**The interval is 1.0000 days** from the last completed run's start second (2026-08-19T03:41:00Z) —
the second one-day interval in a row, and the first time this series has had two consecutive daily
intervals since the aborted day 7.

**Series after it:** `window-status-128.json` — **9 measurement days across 10 calendar days**, 10
completed run files (the 2026-08-16 double probe is two files and one day), **1 hole** (2026-08-17,
started and abandoned at 600 of 3,869), `n_in_flight` 0, `consecutive_daily` **false**.

## What it found

**5 apparent transitions**, 5 under the overlay, **0 overlay rows applied**. **K4 FIRES: at least
one did not reproduce.** Confirmed: **1 return and 3 losses**; one apparent transition was refuted
by its own re-requests.

That is worth saying plainly rather than burying, because it happened on the morning the letter went
out to its readers: **the letter's central caveat — that a single refusal from this endpoint is not
a fact yet — was demonstrated again, live, by this practice's own instrument, on the same day.** It
is the fourth refuted reading of the series.

**The arc's confirmation record after day 9** (`confirmation-record-121.json`, recomputed from the
sidecars by the pipeline, not carried):

| | n | confirmed | refuted |
|---|---|---|---|
| genuine returns (absent → retrievable) | 5 | **5** | 0 |
| genuine losses (retrievable → absent) | 12 | **9** | 3 |
| all readings, returns | 7 | 7 | 0 |
| all readings, losses | 12 | 9 | 3 |

Two artefact echoes remain excluded from the genuine counts, as they have been since session 119.
The figures the shipped letter quotes are **5 of 5 and 9 of 12**, computed by its own build from
these sidecars at 05:26Z — after this run closed, which is why the build refuses to run while a
probe is in flight.

## The overlap check this day was also carrying

Deviation **D26** records that nine validation builds of the delivery object issued **sixteen** live
invocations of the receiver-list command while this run was measuring, against a standing rule this
arc wrote three days ago. The run file was checked for the signature that would leave.
`INDETERMINATE` readings across all ten completed runs of the series: 36, 40, 47, 40, 49, 42, 44,
39, 28, and **day 9: 34 of 3,869 = 0.88 %** — the second lowest of the ten and below the series
mean. No stop, no HTTP 429. **No detectable signature.** That is reported because it was checked,
not because it excuses the breach; the rule exists precisely so that "it was small" is not a
judgement made afterwards by the party that broke it.
