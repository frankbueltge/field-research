# Verifier — the eighth gauntlet, 2026-08-19, published unedited

*Run on `offer/` frozen at `FROZEN-127.sha256` (13 files) before dispatch. The Verifier was told to
check against the sources and the data themselves, never against the reasoning that produced the
object, and was given the whole repository and the open web to check them with.*

---

VERIFIER: FAIL

## Findings

| # | What is wrong | Where | Checked against | Blocking | Correct value / wording |
|---|---|---|---|---|---|
| 1 | The instrument still carries **stale hard-coded confirmation counts**, and they are now wrong. The docstring says the raw readings are "**(5 of 5 returns, 1 of 3 disappearances). Six events is not a rate**". Recomputed from the seven sidecars, the raw readings are **6 of 6 returns and 6 of 8 disappearances — 14 raw readings, 12 genuine events**. The file therefore contradicts `LETTER.md` lines 78–82, which state 6 of 6 raw returns and 6 of 8 losses, in the shipped tool the letter tells a reader to run. | `offer/presence_check.py` lines 102–103 | `ledger/transition-confirm-2026-08-{12,13,14,15,16,18,19}.json`, `ledger/corrections.json`, `offer/measurement.json` `confirmation_record.*` | **BLOCKING** | Remove the parenthetical and "Six events"; the counts move with the series and belong in `confirmation-record-121.json` and the letter, exactly as the two neighbouring paragraphs of the same docstring already say. |
| 2 | The same file's own version note is **false about the file**: "0.3.2 differs from 0.3.1 in ONE thing: the confirmation record was stated in this file as literal counts, and those counts went stale when the series grew. **They are gone**." They are not gone — finding 1 is the surviving instance, in the same docstring, eight lines below the sentence that disowns it. The diff against v0.3.1 removed two of the three literal-count passages and left the third untouched. | `offer/presence_check.py` lines 4–6 vs lines 102–103; diff against `deliverable-v0.3/tools/presence_check.py` | Full `diff -u` of the two files (4 hunks: header note, docstring paragraph, `VERSION` bump, `record_behind_the_default` string) | **BLOCKING** | Either strip the residual counts (then the claim is true) or state that one illustrative count remains and give it currently. As it stands the answer to "does 0.3.2 differ from 0.3.1 in ONE thing only" is **no** — the one thing was done incompletely. |
| 3 | `offer/run_lock.py` is **not the file the daily probe takes its reservation with**. Shipped sha256 `73f4ae47…` (15,867 bytes) is the retired v0.3 bundle's copy. The live file the probe imports is `run_lock.py` in the arc directory, sha256 `abb15cd0…` (19,074 bytes), which contains the whole session-125 L1/L2/L3 repair (`_partial_own_day`, `_is_committed_state`, the completed-run-beside-it leg). The shipped copy is the pre-repair version that falsely refused day 7 on 2026-08-17. | `offer/LETTER.md` line 167: "`run_lock.py` \| the reservation the daily probe takes; imported by ledger.py" | `sha256sum` of all three copies; `run_day8.sh` → `run_window_day.py` (line 40 `import run_lock`) resolving to the arc-directory file; `LOCK-DEFECT-125.md`; `deliverable-v0.3/MANIFEST.json` (which maps the same `73f4ae47…`) | **BLOCKING** | "the reservation module `ledger.py` imports; **this copy is the retired bundle's version and predates the session-125 lock repair**" — or ship `abb15cd0…`. The contrast with line 161, where `ledger.py` *is* certified "unchanged", makes the looser wording read as the same certification. Nothing in the object exercises it: neither `selftest_run_lock.py` nor `selftest_lock_clone_125.py` is shipped, and `presence_check.py` never calls `reserve()`. |
| 4 | Three files are on disk in `offer/` that are in **neither** `BUILD.json`'s table nor the letter's: `__pycache__/{ledger,presence_check,run_lock}.cpython-311.pyc`, mtime 05:29Z — one minute **after** the 05:28Z freeze. `BUILD.json` line 2 asserts "Nothing else may be absent from the table below; the build fails if anything is." 13 files are frozen; 16 are present. | `offer/__pycache__/`, `offer/BUILD.json` `covers`; `../FROZEN-127.sha256` | `os.listdir` vs `BUILD.json.files`; `git check-ignore` (ignored, untracked, so the committed tree is clean) | Non-blocking | Exact recurrence of erratum E23 / `CONDITIONS-126.md` item 3 ("32 frozen, 34 present"). The freeze still verifies **contents and not membership**. These predate this review — they were present at my first `ls`; I ran nothing inside `offer/` and used `PYTHONDONTWRITEBYTECODE=1` throughout. |
| 5 | `figures_fetched` records a `rendered` string that is not what the letter prints. `receiver_dashboard.declared_generation_age_days` → `rendered: "216.3"`; the letter prints "**216 days**". Two further rows record a Python `repr` that appears nowhere in the letter: `today.counts` → `"{'RETRIEVABLE': 10, 'NOT-RETRIEVABLE': 1}"` and `absent_on_both_readings` → `"['7134492331117595950']"`. | `offer/BUILD.json` lines 88–92, 163–166, 224–227 vs `LETTER.md` lines 31, 57, 64 | String search of `LETTER.md`, raw and whitespace/comma-normalised | Non-blocking | The figure is right (216.315 days recomputed) and no number is typed; but `rendered` is a log of the fetched value's repr, not of the letter's text, so it cannot be used to audit the letter's wording. Say so in the field's own description, or make it the printed substring. |
| 6 | "whatever the review returned, including a failure, **is** in the public record for the date on this letter: `journal/2026-08-19.md`" — stated in the present tense about a review that had not run. As frozen, that file exists but ends "*What actually happened is written below, after the fact*" and contains no review outcome. | `offer/LETTER.md` lines 143–146 | `/home/user/field-research/journal/2026-08-19.md`, full read | Non-blocking | "will be in the public record" — or nothing, since the sentence is a commitment, not a measurement. |
| 7 | "Version 1.0 of this object" is asserted by nothing in the object. `BUILD.json` has no version field; no file declares it. | `offer/LETTER.md` line 141 | All 13 files | Non-blocking (informational) | Self-naming only; harmless, but it is the one figure in the letter with no field behind it. |
| 8 | The two completed 2026-08-16 runs are **indistinguishable from their own contents**: both `run-2026-08-16T0337Z.json` and `run-2026-08-16T0337Z-second-probe.json` declare `run_utc_start` = `2026-08-16T03:37:40Z` and the identical `run_id`. `series-status.json` names the second-probe file as the measurement day and the other as the extra pass; `reference-baseline.json` and `measurement.json` name the *other* one as the reference population's source run. | `ledger/run-2026-08-16T0337Z*.json`; `offer/series-status.json` lines 37–42, 58–64; `offer/reference-baseline.json` `source_run` | Direct read of both run files | Non-blocking | No figure moves — the "one day, not two" rule is applied correctly either way and the interval arithmetic is identical. But which file *is* day 6 cannot be settled from the run files, only from the filename. |
| 9 | The letter's "**3,580** identifiers" is `pooled.n`; the age-band weights that actually produce the printed 0.1373 sum to **3,573** (7 undatable rows are in `pooled` and in no band). | `offer/LETTER.md` line 101; `offer/reference-baseline.json` `pooled`/`by_age_band` | Summed the six bands (494+768+791+674+457+389 = 3,573) and the nine years (= 3,573); `n_units_in_run` 3,869 − 249 − 40 = 3,580 | Non-blocking | The number cited is a real field and the population sentence is accurate; the denominator of the comparison figure is 7 smaller than the number quoted beside it. |

## What I recomputed and it was right

**Today's reading, from `offer/your-eleven-today.json`.** Recounted the 11 `observations` myself: `{'RETRIEVABLE': 10, 'NOT-RETRIEVABLE': 1}`, `determinate` 11, absence 1/11 = 0.090909. The single refusal is `7134492331117595950` with five `NOT-RETRIEVABLE` confirmation passes, `n_agreeing` 5, `n_reversing` 0. Every row has `first_pass_state == state`, so `n_unconfirmed_absent` = 0 and the letter's "no first-pass refusal failed to reproduce" holds. `your-eleven-today.json` is byte-for-byte the same object as `measurement.json.today` (`json.dumps(sort_keys=True)` equal).

**The two commands, run by me in a copy under `/tmp`, not in the frozen directory.** `python3 selftest_presence_check.py` → `128 assertion(s) passed, 0 failed`, **exit 0**, no skip lines (`drift-122.json` was beside it). `python3 presence_check.py receiver-list.txt --baseline reference-baseline.json --label the-eleven -o your-eleven-today.json` → **exit 0**, 21.0 s, vantage **AS396982 (US)**, 16 live requests, and the same eleven verdicts in the same order as `BUILD.json`'s stdout tail, including `7134492331117595950 NOT-RETRIEVABLE http=400 conf=+`, `counts {'RETRIEVABLE': 10, 'NOT-RETRIEVABLE': 1}`, `observed public absence 0.0909 (1/11)`, `expected … 0.1373 [0.1135, 0.1651] on n=11`, `frozen-reference drift +0.0000 pp over 3.1 day(s)`. Both of the letter's printed commands run and exit 0.

**The series, recomputed from the run files themselves, not from `series-status.json`.** Nine files match `run-*.json` and none is a `.partial`; their start seconds fall on eight distinct UTC days (08-11, 12, 13, 14, 15, 16, 18, 19), 08-16 carrying two completed runs → **8 measurement days**, matching `n_measurement_days`. `run-2026-08-17T0337Z.json.partial` has 600 of 3,869 units and no completed file beside it → **1 hole**, and it is the only `.partial` without one (the other six all sit beside completed runs). First start `2026-08-11T11:24:06Z`, last `2026-08-19T03:41:00Z`, inclusive span 08-11…08-19 = **9 calendar days**. Recomputed all seven intervals from the raw start seconds: 0.678, 1.0323, 0.970, 0.9958, 1.0, 2.0023, 1.0 days — matching `intervals_days` to four places, two of them not daily, so `consecutive_daily` **False** is correct and is printed as such. `offer/series-status.json` is byte-identical to `window-status-127.json`.

**The confirmation record, recomputed by hand from the seven sidecars and `corrections.json`.** Raw transition readings: NOT-RETRIEVABLE→RETRIEVABLE **6, all 6 confirmed**; RETRIEVABLE→NOT-RETRIEVABLE **8, 6 confirmed, 2 refuted**. The two refuted are `7368171405361351954` (08-13) and `7016669364938149122` (08-14) — both refused once and then returned RETRIEVABLE on all five re-requests seconds later, both carried as the only two rows of `corrections.json`. The two artefact echoes are those same two identifiers' subsequent "returns" (08-14 and 08-15), so genuine returns are **4 of 4** and genuine losses **6 of 8**: **10 confirmations out of 12 genuine events**, exactly as the letter states, with `n_sidecars` 7 correct. Day 8's own sidecar (05:27:18Z) is inside the build (05:27:33Z), so the cut-off claim holds.

**The reference population and its date.** `reference-baseline.json` `pooled.n` = **3,580**, `t_ref_utc` = `ages_computed_at_utc` = **2026-08-16T03:37:40Z** (so `clock_check: AGREE` is earned). Recomputed the printed expectation from the band table and the list's own age histogram (2-3y: 7, 3-4y: 2, 4-5y: 2 — which I re-derived from the eleven `age_y` values): (7×0.122630 + 2×0.161721 + 2×0.164114)/11 = **0.137280**, and the interval bounds the same way = **[0.113519, 0.165076]** — matching to all printed digits. The bracket: `2026-08-01T22:33:14Z` → `2026-08-11T11:24:06Z` = **9.5353 days**. "37 language editions" is supported: 45 `corpus-*.wikipedia.org.json` files were swept, 8 are 135–136-byte empty results, **37** returned content.

**The dashboard — checked against the live page, not only the saved bytes.** I fetched `https://playground.tiktok-audit.com/api-na/` myself just now: HTTP 200, 246,014 bytes, sha256 **fff0a66f2bddc05106b892f7d18d59202eda1ab6829f71da7edbfea624f9c6bb** — identical to both `receiver-dashboard-2026-08-19.html` and `receiver-dashboard-2026-08-16.html`, so the letter's truncated `fff0a66f2bddc051…` and its "the bytes are identical" both hold against the live source. Stripping tags from the saved HTML recovers verbatim: "Dashboard generated on: 2026-01-14 21:53:41", "11 Total Videos Tracked 0 Available Videos 0 Unavailable Videos 11 Videos with Errors", and "Note: Error are problems on our end, not TikTok." All eleven identifiers appear in the page. Declared-generation age recomputed: 2026-01-14 21:53:41 → 2026-08-19 05:27:33 = **216.315 days**, so "216 days" is right.

**The prior reading, recomputed rather than read.** Extracted the 2026-08-12 states from `deliverable-v0.3/receiver-eleven.json` `units` and joined them to today's: 11 shared identifiers, **0 changed state**, prior counts `{'RETRIEVABLE': 10, 'NOT-RETRIEVABLE': 1}`, absent on both readings = `['7134492331117595950']`, prior `started_utc` `2026-08-12T18:35:26Z`, same vantage AS396982.

**The register check, against the live register.** Fetched `https://frankbueltge.de/papers/index.json` myself: `count` **1131**, `entries` length **1131**, sha256 **9319bc61855394f54beb402b25c7ece08a32efb33d1cada7e5997b11cd844a8a** — the same hash `neighbours-127.json` recorded at 03:56:23Z. Prior count 1,116 at 2026-08-15 → growth **15**.

**The neighbour claim, first-hand.** `arXiv:2601.12390` retrieved: Bekavac & Mayer, DOI `10.1145/3805689.3812237`, journal reference *Proceedings of FAccT '26*, submitted 2026-01-18T12:59:11Z — so "FAccT '26; preprint arXiv:2601.12390" is one work, not two, despite the house register listing it under two titles. Abstract: "reconstruct full information feeds for two controlled sockpuppet accounts during two election periods and benchmark these against the data retrievable for the same posts through the corresponding Research APIs… the TikTok Research API and the Meta Content Library." The letter's one-sentence description of it is accurate, and "run *through accounts*, over bounded periods, and published as a study" is supported by that abstract.

**The receiver's quoted words.** "should be available through the Research API but were not" is present verbatim in `receiver-report-2506.09746v2-extracted.txt` (whitespace-normalised; it wraps across lines in the extraction), in the sentence "We decided to create a public dashboard… We set up a script to regularly check a set of videos that should be available through the Research API but were not."

**The file inventory.** All 12 `BUILD.json` rows verified by recomputing sha256 and byte length — 12 of 12 match on both. All 13 rows of `../FROZEN-127.sha256` verify. Nothing in the table is missing from disk. The letter's own file table lists exactly the 13 files present, in the same names (see finding 4 for the three `.pyc` that are on disk and in neither table). `offer/ledger.py` is byte-identical (`05554b29…`) to both the arc's working `ledger.py` and `deliverable-v0.3/tools/ledger.py`, so "unchanged and not re-implemented" is exact. `offer/drift-122.json` is byte-identical to the arc's `drift-122.json`, and `drift-122.json` is checked by exactly **four** assertions in the selftest (lines 366–374) — the letter's "four of the suite's assertions" is right.

**Every `figures_fetched` row against its named field.** All 44 rows: the `raw` value equals what the named field of the named file actually holds, including the two `series-status.json` rows and the negative index `measurement_days.-1.start_utc`. No field is missing or renamed. 41 of 44 `rendered` strings appear in the letter after whitespace/comma normalisation; the three that do not are finding 5.

**Other letter statements checked and supported.** `letter_words` 1,710 = `len(text.split())` on `LETTER.md`. "32-file bundle": `deliverable-v0.3/` holds exactly **32** files excluding `__pycache__`. "failed… seven times — never on a measurement, always on its packaging": `WORKBOARD.md` line 11 and `journal/2026-08-19.md` both state seven consecutive failed gauntlets, all in the packaging. `journal/2026-08-19.md` exists. Licences match `LICENSE.md` (code Apache 2.0, text CC BY 4.0, data CC0 1.0). `https://github.com/frankbueltge/field-research` → HTTP 200 (public); `https://frankbueltge.de` → HTTP 200. "Meridian, an autonomous research practice" and Frank Bültge as architect are in `PROTOCOL.md` and `README.md`. No credential, token, cookie or `Authorization` header appears anywhere in `presence_check.py` or `ledger.py`; the only word matched was "credential-free" in a comment, and the saved `vantage` block holds only mode, ASN, country and the lookup's own disclosure note.

**No claim in `LETTER.md` was found to lack a source or a computed file.** I went through it sentence by sentence; every factual assertion resolves to a field in `measurement.json` / `series-status.json` / `reference-baseline.json`, to a ledger file, to a repository record, or to a live URL I fetched. There is no unsourced assertion and no place where a "conjecture" marking is missing — the one judgement in the object (`which_one_is_defensible` in `measurement.json`) labels itself "a judgement of this practice, defended in prose and never tested against a second observation", and it is not repeated in the letter.

## What I could not check, and why

- **Whether the 2026-08-19 dashboard HTML was genuinely re-fetched at build time**, rather than copied from the 2026-08-16 save. The two files are byte-identical, which is exactly what "unchanged" looks like, so the two cases are indistinguishable from the artefacts. My own live fetch returns those same bytes, which makes the letter's claim true as of now regardless of how the file was produced — but it does not prove the build fetched it.
- **That the 2026-08-12 prior reading actually happened at 2026-08-12T18:35:26Z.** I verified `deliverable-v0.3/receiver-eleven.json`'s contents and their agreement with today, but its own timestamp is a declaration in a file, not something reproducible now.
- **The 2026-08-15 register count of 1,116.** The register is live and now holds 1,131; the earlier figure is attested only by `NEIGHBOURS-120.md` and `neighbours-127.json`, both in-house.
- **`10.1145/3805689.3812237` at the ACM.** `NEIGHBOURS-120.md` records the publisher page returning 403 from this vantage; I did not re-attempt it. The DOI is corroborated instead by arXiv's own metadata for 2601.12390, which carries that DOI and the FAccT '26 journal reference — which is enough for the letter's claim.
- **Whether the panel-construction bracket of 9.5353 days is the tightest available bound.** I verified the arithmetic between the two declared bounds; whether those bounds are the right ones rests on `panel-date-125.json` and the arc's reconstruction, which I did not re-derive.
- **Whether five confirmation passes is the right number.** The object says plainly that nothing has measured what a sixth pass or a delayed pass would add, and I have no evidence either way.
- **The claim that this letter has not been sent and nobody named in it has been contacted.** Unfalsifiable from inside the repository.
- **Who wrote the three `.pyc` files in `offer/`.** They existed before my first command (mtime 05:29Z, freeze 05:28Z) and are git-ignored, so nothing in the repository records their author.
