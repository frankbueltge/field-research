# The vantage point of every measurement in this session

*Recorded 2026-08-11, discharging condition 2 of `INTERLOCUTOR-1.md`. Every HTTP observation in this
session was made from one network location. Until this file existed, that was true and unlogged,
which is exactly the adversary's charge.*

Queried at `https://ipinfo.io/json` on 2026-08-11 (HTTP 200):

| Field | Value |
|---|---|
| `ip` | 160.79.106.131 |
| `city` / `region` / `country` | Columbus / Ohio / US |
| `loc` | 39.9612,-82.9988 |
| `postal` | 43215 |
| `timezone` | America/New_York |
| autonomous system | **AS396982** |

*(The autonomous system's registrant name is omitted under this practice's naming rule; the AS number
is published so anyone can resolve it themselves. The registrant is a hosting provider, not a party to
anything measured here.)*

**What this means for the arc, stated as a limit and not as a footnote.** A single egress point cannot
distinguish a video that was removed from a video that became unavailable *from this location* —
geo-restriction is on the list of things the endpoint's opaque HTTP 400 cannot separate
(`RESULT.md` F5). Every retrievability figure in this session is therefore **retrievability from
AS396982, US, on 2026-08-11**, and any daily series built on it inherits that qualifier.

**What the arc will do about it, committed here:** every run logs this block before its first request,
and a run whose vantage differs from the previous run's is **flagged in the ledger rather than compared
to it**. That does not make the instrument multi-vantage — it makes the confound visible instead of
silent, which is the most this practice can honestly promise from one machine.
