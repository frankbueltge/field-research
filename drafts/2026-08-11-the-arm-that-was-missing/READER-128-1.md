# Severed reader 1 — full answer, unedited

*Session 128, 2026-08-20. Entry point: `LETTER.md`. Severing conditions and the panel's own
arithmetic and limits: `READERS-128.md`. This file is the reader's answer as returned, including
its opening line about the state of its working copy. Nothing is edited, reordered or cut.*

---

Good, directory restored to its original state. Here is what I found.

1. **The single most important thing this directory tells you.** All eleven video-status timelines on a public TikTok-research dashboard (at `playground.tiktok-audit.com`) flipped to "Error" on the same day, 2026-01-03, and the dashboard's own record has not moved since 2026-01-14 — 218 days before this was written — even though its front page still displays those errors as if current.

2. **What is it about?** It's an evidence bundle arguing that a third-party "TikTok Research API" monitoring dashboard is silently stuck: its per-video error state, read by a script out of the page's own chart data, all changed on one date and then stopped updating entirely, while a fresh check this morning found most of those same videos are still publicly reachable through TikTok's oEmbed endpoint. The letter is explicit that this doesn't prove what broke or that the dashboard's underlying research-API problem is fake — only that the displayed "all errors" state describes January, not today, and that most of the videos it calls errored are not unreachable from the outside.

3. **Who is it for, and who is it from?** It's addressed to whoever runs that dashboard ("your dashboard," "your record") — presumably the AI Forensics team or dashboard operator, though never named as recipient. It says it was "written and measured by Meridian, an autonomous research practice" (i.e., an AI system did the measuring and writing), with a named human, Frank Bültge, stated as the one who "publishes it and carries responsibility for it." The letter itself says plainly it has not been sent and nobody has been contacted.

4. **What would you not trust, or want to check yourself?** The letter's own "What this cannot tell you" section already flags the big ones, and they're worth taking seriously: a morning reading in August can't characterize a January state; a video being publicly fetchable via oEmbed doesn't mean it's genuinely present in whatever the research API interface actually checks, so it can't rule out a real interface gap; the oEmbed "not retrievable" code is one opaque signal that doesn't distinguish deletion from anything else. Beyond that — two of the figures quoted (the 5x re-request confirmation counts, and the "9 measurement days" ledger) are computed from files the letter admits are not in this directory, so those specific numbers are asserted, not independently checkable here. I did run the reproducible pieces myself: the self-tests pass, the dashboard file's hash matches what's recorded, and regenerating `receiver-series.json` and `dashboard-findings.json` from the raw HTML reproduced them byte-for-byte — so the arithmetic and extraction the directory can prove, it does prove.

5. **Where, if anywhere, did you stop reading or want to stop?** I read everything, but the place I'd flag as a natural stopping point for a skeptical reader is the "Terms, and who answers for this" section — the acknowledgment that this object has predecessors with a public record of "every review this object and its predecessors failed." That's a claim about a track record of past failures that this directory doesn't contain evidence for one way or the other; you'd have to go to the named external repository to see it, and this bundle doesn't ask you to just take its word for the current numbers, but it does ask you to take its word for that history.

6. **Anything you noticed that nobody asked you about.** The self-scrutiny is unusually heavy for a document like this — it flags its own weaknesses (denominator mismatches with a quoted external claim, a percentage figure it says explicitly did NOT reproduce, a "consecutive daily" claim it labels false in its own status file, a privacy note that running the tool as shipped leaks the runner's IP to a lookup service unless disabled) rather than only the findings that support its headline. It also visibly refuses to over-claim: one line reads "We are not saying what broke — only that whatever it was, it happened on 2026-01-03," and another explicitly declines to certify its own 5-re-request methodology as correct, just as the one it precommitted to. That tone — hedging against its own conclusion almost as hard as it argues for it — was the most notable thing in the directory, more than any individual number.
