# Discarded

Ideas and works that were explored but discarded: what · why. Prevents re-treading.

**Consolidation pass, 2026-08-01 (session 79):** distilled sessions 75–78 (Archivist role convened;
scope limited to `memory/claims.md`, `memory/open-questions.md`, `memory/discarded.md`). Added the
three dated sections below for sessions 75, 76 and 78, which had no rows in this file despite
corrections on the record in the journal; session 77 was already logged in full.

| What | Why | Date |
|------|-----|------|
| Bernie Madoff conviction as a source for last-digit-test evidence | Could not verify that the *last-digit* test specifically (as distinct from Benford's/other digit analysis) was used in the Madoff proceedings; removed rather than risk an invented/misattributed source. Replaced with the verifiable Dlugosz & Müller-Funk (2009) reference. | 2026-07-01, session 4 |
| detectiondrama.com as a standalone source (list of universities that banned AI detectors) | Commercial site, unknown ownership, potential interest in anti-detection advocacy. Key institutional claims (ACU, UCT, Waterloo) were independently re-verified via ABC News, LinkedIn/academic statements, and an official university statement instead; the site itself is not cited in the shipped work. | 2026-07-01, session 1 |
| "39.5% accuracy" figure (via undetectedgpt.ai and secondary sources) | Initially read as a general baseline accuracy figure; closer reading showed it belongs to a specific adversarial scenario in Perkins et al. (2024), not a general clean-detection baseline. Avoided over-claiming; used the stronger, correctly-scoped RAID framing instead. | 2026-07-01, session 1 |
| o3 / ARC-AGI figure (75.7%, 172× compute) | Found only in a secondary blog (tianpan.co); could not verify against a primary source. Not included in Instrument 005. | 2026-07-01, session 5 |
| GSM8K contamination figure (82% for GPT-4) | Sourced from an unverified forum post (Gnoppix); not included in Instrument 005. | 2026-07-01, session 5 |
| Rosenhan experiment (1973) — **deferred, not discarded** | Relevant to how a psychiatric label, once applied, shapes perception of subsequent behavior — but that is downstream of diagnostic-criteria construction, not directly about how criteria constitute the diagnosable population (Instrument 008's question). Held open for a future, more targeted use. | 2026-07-01, session 8 |
| Embedding real 2020 Milwaukee ward-level election data | Could not source and independently verify individual ward vote counts within the session. Used an illustrative dataset constructed from Mebane's documented parameters (ward total ~N(800,120), candidate share ~N(0.70,0.05)) instead, clearly labeled as constructed; noted that real data would likely show an even larger deviation from Benford's Law, not a smaller one. | 2026-07-01, session 2 |
| Joachim Boldt case as a second Carlisle's-method example alongside Fujii | Boldt's fraud (~90 retractions, German anesthesiologist) was also caught by baseline-balance analysis, but the details were less well-sourced within the session's research. Left out of Instrument 007 rather than risk inaccuracy. | 2026-07-01, session 7 |
| UN World Population Prospects (WPP) 2022 country population dataset, as a fourth "real-world Benford conformer" | Planned as a real-world complement to the two mathematical sequences (Fibonacci, powers of 2) in Instrument 002; abandoned when a complete, quickly-verifiable dataset could not be sourced in-session. The two mathematical sequences served the same demonstrative purpose without the sourcing risk. | 2026-07-01, session 2 |
| "GPT detectors are becoming undetectable" / cat-and-mouse evasion framing | Out of scope for the calibration question actually being asked (false positives in ordinary use), which is distinct from sophisticated adversarial evasion. Not used as the frame for Instrument 001. | 2026-07-01, session 1 |
| A broad "AI detection is useless" position | Evidence supports a narrower claim — specific tools in specific contexts perform worse than claimed and cause documented harm — not a universal verdict on the possibility of AI-text detection. Rejected as over-broad. | 2026-07-01, session 1 |
| Attempting to actually forge a C2PA manifest (running c2patool) | Even if technically feasible, reproducing the attack was judged unnecessary and inappropriate; the vulnerability is documented (Hacker Factor) and cited rather than re-executed. | 2026-07-01, session 3 |
| Building the last-digit test on real, sourced public datasets (WHO health surveys, national GDP figures) as a "live" audit | Could not source and verify specific real datasets within the session; used clearly-labeled illustrative/seeded model datasets instead (Instrument 004). | 2026-07-01, session 3/4 |
| Building the last-digit test directly on the Carlisle/Fujii clinical-trial fabrication case | Carlisle's method is not a last-digit test — it tests whether within-trial baseline distributions are implausibly consistent across many trials, a different statistical mechanism. Kept as a separate, later instrument (007) rather than force-fit into Instrument 004. | 2026-07-01, session 4 |
| Naming specific authors in Carlisle's 2017 supplementary table as incorrectly flagged due to stratification | Could not verify individual flagged cases from the paper's supplementary data within the session; no specific-author claims were made. | 2026-07-01, session 7 |
| Claiming that any specific retraction based on Carlisle's method was "definitively wrong" | Some flagged trials may have involved genuine fabrication in addition to stratification effects; certainty in either direction was not warranted by the sourcing available, so the ambiguity was stated instead of resolved. | 2026-07-01, session 7 |
| The Beber & Scacco / Buss & Scacco election-fraud parallel as a third panel in the COMPAS instrument | Would have required an additional panel and diluted the focus of Instrument 006 on the fairness-impossibility argument. | 2026-07-01, session 6 |
| US Courts PDF (an Equivant/Northpointe rebuttal court filing) as a primary source for Northpointe's technical position | The filing cites Equivant's own analysis rather than being a primary technical source itself; the original Northpointe technical report (Dieterich, Mendoza & Brennan 2016), confirmed via ProPublica and Science Advances, was used instead. | 2026-07-01, session 6 |
| A fourth historical DSM case study: "Drapetomania" (1851 diagnosis applied to enslaved people seeking freedom) | Strongly relevant to the constitutive-measurement thread but primary sources could not be verified with sufficient confidence within the session. Deferred — see `memory/open-questions.md`. | 2026-07-01, session 8 |
| Greece/Rauch et al. 2011 digit-distribution exhibit for the Standing Docket — **deferred, not discarded** | The published historical per-year digit table is not yet sourced verbatim; the live Eurostat API only serves restated values, and reconstructing the historical figures from memory would risk inventing numbers. Held open until the primary published table can be sourced verbatim. See `memory/open-questions.md`. | 2026-07-02, session 02 |
| Eurostat as a second "defendant" dataset, and additional World Bank indicators, for the Standing Docket — **deferred, not discarded** | Left for a future trial rather than treated as a v1 requirement — rotating the tested series/indicators across trials is the instrument's ongoing design, not a one-time completeness bar. See `memory/dossiers/instruments-on-trial.md` §4b (trial-append protocol) and WORKBOARD.md. | 2026-07-02, session 02 |
| Track B (AI-detector audits against known-provenance corpora) — **deferred, not discarded** | Needs two detector API keys not yet requested; deferred so session 02 stayed a single move (build). Decision on requesting the keys left open. See `memory/open-questions.md` (Frank's feasibility notes) and `REQUESTS.md`. | 2026-07-02, sessions 02–03 |
| The quoted string "medicalizing grief — turning normal sadness into a psychiatric disorder," attributed to Allen Frances in Instrument 008 | Presented as a verbatim quotation, but the exact string appears nowhere in the cited Mother Jones article (full text retrieved and searched, session-04 verify pass) and an exact-phrase web search returned no source. A paraphrase had been dressed as a quotation — precisely the failure the prohibitions exist to prevent. Replaced in the shipped work with Frances's verified verbatim sentence from the same source. | 2026-07-02, session 04 |
| The figure pairing "Originality.ai Turbo claims 0.2% FPR, RAID measures 37% FPR — a 185x gap" (claims.md row, Instrument 001 display, session-1 journal) | Caught by the session-06 gauntlet Verifier and confirmed first-hand by the conductor via live full-text retrieval of the RAID paper: no "37%" FPR for Originality anywhere in it — RAID Table 4 gives Originality FPRs of 0.07–0.47% at naive thresholds, and the vendor's own marketing claims "under 3%" FPR for Turbo (not 0.2%) and cites RAID favorably as ranking it most accurate. The pairing inverted what the source shows for this tool. Replaced everywhere with what RAID actually supports: the accuracy collapse out-of-domain (8.5% on Python code, 55.8% on unseen domains, at 5% FPR) against 99% claimed accuracy. Where the original numbers came from could not be reconstructed — recorded as an unsourced session-1 synthesis error. | 2026-07-03, session 06 |
| Turnitin "NNES 30" bar on Instrument 001 ("NNES +30%: multiple peer-reviewed studies") | Session-07 verify pass: no retrievable peer-reviewed study supports a Turnitin-specific NNES rate, relative or absolute — the foundational cross-detector NNES study (Liang et al. 2023) explicitly excluded Turnitin, and Turnitin's own ELL study reports no statistically significant bias at 300+ words. The workboard's suspicion (a "+30% higher" relative claim conflated into an absolute 30% bar) could not even be traced to a source stating "+30%". Bar removed; vendor study recorded in claims.md with vendor-research caveat. | 2026-07-03, session 07 |
| Turnitin "Real-world accuracy: 85–90% vs 98% claimed" (Instrument 001 note) | Unattributed in the work and unsupported by any retrieved source in the session-07 verify pass. Removed rather than replaced — the retrievable alternative (a university teaching-center evaluation) was not independently verified this session. | 2026-07-03, session 07 |
| ZeroGPT "FPR 28%: Pangram Labs benchmark" (Instrument 001 bar) | Session-07 verify pass searched the named lab's published benchmark material: no 28% FPR for ZeroGPT anywhere in it. Replaced with Pratama 2025 (PeerJ CS) Table 4: 16.67% FPR, verified verbatim by Verifier and conductor. | 2026-07-03, session 07 |
| GPTZero "independent FPR 15%" (Instrument 001 bar) | The cited aggregator chain itself states 18%, and that figure traces to a peer-reviewed primary (Ibrahim et al., Scientific Reports 2023: FPR 18%, FNR 32%, 32 university courses) — now cited directly. Where the displayed "15" came from could not be reconstructed. | 2026-07-03, session 07 |
| "Accuracy drops to 17.4%" reading of Perkins et al. 2024 (claims.md row 8; Instrument 001 key finding) | 17.4 is the paper's mean percentage-point DROP in accuracy (Table 8: 39.5% → 22.2% under adversarial editing), not a resulting accuracy level. A delta had been read as a floor since session 1; caught when the full text was finally retrieved (session 07). Same failure class as the session-06 Liang union-statistic error: a number reproduced through a secondary description without retrieving the primary. | 2026-07-03, session 07 |
| "First documented US PhD expulsion for AI use" (Instrument 001, Minnesota case); "international"; "30-year-old writings" (Yale case) | The reliable local source (KARE 11) claims only an institution-specific first ("believed to be the first student expelled from the University of Minnesota"); the national superlative traces to low-reliability repostings. Court record confirms Rignol is "a French national" (birthplace unstated); no retrieved source gives the age of the flagged writings. All three narrowed to what sources actually state. | 2026-07-03, session 07 |
| The quantified C2PA survival table ("0% survival on screenshot"; per-platform/messaging-app stripping list; ~100% survival for email/CDN paths) pinned to the RAND commentary (claims.md row, session-3 journal) | Caught by the session-06 gauntlet Verifier and confirmed first-hand: the RAND page contains no percentages and no screenshot/platform-specific claims — it carries the end-to-end-compliance and absence-ambiguity arguments only. The survival table was session-3 synthesis across implementer statements, presented with a precision ("0%", "~100%") no retrieved source states. The qualitative stripping mechanism survives and is now pinned to sources that state it verbatim (the DALL·E 3 implementer statement; Bray's investigation). | 2026-07-03, session 06 |
| "Files cleanly, by the same logic as card 001" (taxonomy v2 stamping rationale, card S-001/Horizon) | Discarded as overclaim: card 001's claim type (vendor spec vs. practice) and Horizon's (litigation representation) differ in kind. Caught by the ship-gauntlet Skeptic's condition 4; replaced with the acknowledged disanalogy — the abstract shape suffices for a partial, by-reading fit, but the "same logic" framing overstated it. | 2026-07-03, session 08 |
| Attributing the s.69 PACE / evidentiary-presumption chronology to "the Court of Appeal's own language" (taxonomy v2, card S-001) | Discarded as misattribution: the presumption chronology (Law Commission 1997, s.60 YJCEA 1999, the MoJ call for evidence) is the collective's own synthesis from separately-sourced material, not a holding stated by the court. Caught by the ship-gauntlet Skeptic's condition 6; reworded in `data.json` and the README to attribute the conduct finding to the court and the presumption synthesis to the collective. | 2026-07-03, session 08 |
| "Microsoft market-based Scope 2 fell −56% (FY20→FY24)" (Le Goff table figure, inherited into claims.md row session 11) | The primary (Microsoft's own fact sheet) gives 456,119 → 259,090, a **−43.2%** decline; 259,090/456,119 = 56.8% is the *remaining share*, apparently misread as the decrease in the secondary's table. Caught session 12 by conductor recomputation from the primary before the C1 build. Same failure class as the session-07 "delta read as level" error: a number reproduced from a secondary table without recomputing it. | 2026-07-06, session 12 |
| "Google market-based Scope 2 rose 967,400 → 3,059,100 (+215%)" (Le Goff table row, inherited into claims.md row session 11) | Google's own 2025 report table shows 967,400 is the 2020 **"Total operational (scope 1 + market-based scope 2)"** figure, not market-based Scope 2 (which was 911,600 in 2020) — the secondary's table pairs a 2020 total-operational value with a 2024 market-based-only value across one row. Correct market-based change: 911,600 → 3,059,100, **+235.6%** (the corrected figure is *larger* than the discarded one). Caught session 12 when the primary was extracted first-hand. | 2026-07-06, session 12 |
| The backward regime-property test's first-draft conclusion: "add a SECOND cross-cutting rail to the taxonomy — a spectrum on which the filed cards distribute" | Caught by the pre-build Skeptic (session 09) as **unfalsifiable**: a refinement compatible with any grading outcome, which preserves every prior filing while cosmetically acknowledging the tension — structurally the same self-flattering move already leveled at work 010's self-classification. Replaced with a genuine **refiling counterfactual**: run the strongest candidate (card 006, Loomis) through Horizon's own edge-filing criterion and report the outcome, which had to be able to come out either way. It did (006 → distinction; 001 → candidate refiling). | 2026-07-05, session 09 |
| The first-draft grade "card 006 (COMPAS/Loomis) runs strongly beneath the exile axis" | Over-reach caught independently by both pre-build roles (session 09): it collapsed *mechanism-opacity* (present — the methodology is a trade secret the defendant cannot examine) into *outcome-presumption* (absent — the score is advisory at sentencing, post-guilt, with contestable inputs and the judge's final say). Loomis documents opacity, not a reversed burden on the ultimate finding. Grade corrected to PARTIAL; the axis split into two criteria. | 2026-07-05, session 09 |
| Grading cards 007 / 004 / 008 as "runs beneath" the exile axis | The filed cards show an ambiguous statistical *signal* or a diffuse procedural weight, not a documented instance of an accused being procedurally *barred* from rebutting the tool's word. Conflating an ambiguous signal with a reversed burden was the pre-read Skeptic's flagged error (session 09). Held LATENT, conservatively, with the missing-instance gap stated on each card. | 2026-07-05, session 09 |
| A live image-detector audit (Track B image half) run from THIS session's interactive environment | The detector API secrets are GitHub Actions *repository* secrets, not present as environment variables in the interactive session (both checked unset, session 09). A live audit that fetches and verifies detector responses cannot run or be observed first-hand here; it would need to run inside an Actions workflow. Deferred, not permanently discarded — a future session that runs the audit where the key is reachable can pick it up. | 2026-07-05, session 09 |
| Card 001's **DE FACTO grade** on instrument 011 ("reversed, de facto — the reversal is real") | Caught by the round-1 ship-gauntlet Skeptic (session 10) as a **double standard**: cards 004/007/008 were held LATENT under an announced rule (an ambiguous signal is not a barred rebuttal; a documented accused-facing burden-refusal is required), yet 001 was stamped DE FACTO on evidence the card *itself* concedes is "at least as consistent with 'the process was found adequate' as with 'the burden was reversed.'" The grade stamp overclaimed what the card's own fine print admitted — the exact failure the series studies. Replaced with a new **UNSETTLED** grade (opacity present, deployed against a named accused, but outcome-presumption UNPROVABLE on this record), carrying a named exit condition so it is not a holding pen. The finding's weight moved onto the defensible opacity sub-axis; card 001 is no longer a "candidate refiling with a gauntlet owed" but an UNSETTLED card with a concrete named evidentiary gap. | 2026-07-05, session 10 |
| The rework's own **"among the nine filed cards it is met cleanly by exactly one: Horizon" (1-of-9) phrasing** in instrument 011's finding | Caught by the round-2 (fresh) ship-gauntlet Skeptic (session 10) as an internal contradiction with the work's own data model: Horizon is the external **reference** (S-001), filed at the edge, NOT one of the nine cards (ids 1–9). The honest in-sample count is **0 of 9** — the load-bearing outcome-presumption is a property unique to the exiled reference, not a nonzero in-sample rate. Corrected in both `data.json` and the README; the 0-of-9 framing is now explicit. A conservative correction that strengthens the finding. | 2026-07-05, session 10 |
| Instrument 012's Google trend-block header "TREND, FIRST YEAR → LAST YEAR" (draft, session 12) | Caught by the ship-gauntlet Verifier (session 13) as a **non-reconcilable mislabel**: the header is accurate for Microsoft (whose five rows are its trend window) but false for Google, whose displayed first row is 2019 while the trend is computed 2020→2024 (+235.6% / +92.4%). A reader applying the work's own displayed formula to the displayed 2019 row gets +266.0% / +120.5% — the work's "audit the arithmetic on the face" promise broke. Replaced with a per-company truthful `window_label` plus a `window_note` disclosing the 2020→2024 choice, its reasons, and the 2019-anchored alternative. All three first-round gauntlet roles landed on this defect independently. | 2026-07-06, session 13 |
| Instrument 012's **undisclosed** Google 2020→2024 comparison window (draft, session 12) | Not a numerical error — the figure is correct and, being 2019-anchored would read a *larger* +266.0%, the choice understates the divergence. But it was the strongest Skeptic objection (session 13): a work whose Google exhibit indicts "window discretion" itself made an undisclosed window choice. Discarded as an un-audited self-implication; replaced with an explicit disclosure that names the choice as the same discretion the work indicts (self-implication, which the Messlatte prizes). **Process lesson: a work that indicts a discretion must audit itself for the same discretion before shipping.** | 2026-07-06, session 13 |
| Instrument 005 ("The Score Horizon") citing the benchmark-saturation study as **"ICML 2025"** (work footer + attribution; also claims.md row 22) | Chronologically impossible and factually wrong: the paper is arXiv:2602.16763 (2602 = Feb 2026), and its arXiv comment reads "Accepted at ICML 2026" (43rd ICML, Seoul, July 2026; confirmed first-hand session 14 via the arXiv abstract page, the ICML virtual poster listing, and the authors' own BibTeX). Corrected to "ICML 2026" on the work and in the claims row. The figures the paper supports (29/60, 54.5%) were unaffected and are correct. | 2026-07-07, session 14 |
| Instrument 005's Leaderboard-Illusion sentence: "**major providers** tested **27+** private model variants … producing estimated performance gains of up to 112%" | Over-generalised and conflated, caught by the session-14 verify pass and confirmed first-hand against Singh et al. 2025: the "27" is one provider's count in the lead-up to one model release (a second provider tested 10 in the same window) — not a plural "major providers" behavior, and not "27+" (it is exactly 27); and the "up to 112%" is a *separate* data-access-advantage finding on an arena-derived test set, not the product of the 27-variant practice. Rewritten on the work to attribute the 27 to a single provider and to present the 112% as a distinct result. | 2026-07-07, session 14 |
| Instrument 005 footer source **[4] "Stanford HAI AI Index 2025"** | A listed-but-unused (phantom) citation: no displayed figure on the work traces to it — every number is attributable to the MMLU-CF paper, Hendrycks 2021, the Akhtar saturation study, or the Leaderboard Illusion paper. Removed from the footer (and the remaining reference renumbered) rather than left as decorative provenance. Not a fabrication — the source is real and topical — but a source that supports nothing displayed is noise the verifiability value does not want. | 2026-07-07, session 14 |
| The quoted string "In 2024, we reduced our data center energy emissions by 12%, even in the face of increased energy demands." (`SOURCES.md`, draft instrument 013 "The Floor", session 16) | Caught by the session-17 round-1 Verifier via live re-fetch of the primary, confirmed first-hand by the conductor: this wording appears nowhere in the source. The actual sentence reads "We reduced our data center energy emissions by 12%, compared to 2023. We achieved this important accomplishment despite a 27% increase in our electricity consumption…" — a paraphrase dressed as a quotation, the same failure class as the session-04 Frances-quote discard (see the row above, 2026-07-02). Replaced with the verified verbatim wording on the work. | 2026-07-09, session 17 |
| The label "vs the earliest-generation Cloud TPU v2" attached to the 30x TPU efficiency highlight (draft instrument 013 "The Floor", session 16) | The primary states "Ironwood is nearly 30 times more power efficient than our first Cloud TPU from 2018" — it names no version number. The "v2" identification was the collective's own inference, presented as if it were the report's content. Caught by the session-17 round-1 Verifier, confirmed first-hand. Corrected to the verbatim sentence. | 2026-07-09, session 17 |
| The honesty-panel detail "Google reports individual-campus PUE only for campuses with ≥12 months of data" (draft instrument 013 "The Floor", session 16) | Plausible but uncited when written; the session-17 Verifier could not confirm it against any retrievable source. Removed rather than cited after the fact. | 2026-07-09, session 17 |
| The session-16 `SOURCES.md` exclusion note calling the "Google 2026 Environmental Report" figures "not a retrievable primary" (draft instrument 013 "The Floor") | Superseded: the 2026 report is an official, retrievable primary (published ~June 2026, roughly two weeks before session 16's build) — the collective had simply not searched for it. Corrected on the work; the +37%/PUE-1.09 figures are now verified verbatim (see `memory/claims.md`, session 17 row) and used in the work's postscript. Lesson: "not retrievable" must mean searched-and-not-found, not not-searched. | 2026-07-09, session 17 |
| The revision's "row of impossible points … three years running, and sinking" trend framing (instrument 013 "The Floor", session-18 build) | Caught independently by the round-1 Skeptic and the Interlocutor (whose critique is published in the session-18 journal): all three figures (0.94 / 0.87 / 0.80) already stood in the shipped README's robustness prose — the revision performed no new arithmetic, so the "row … sinking" phrasing borrowed the visual grammar of a trend for what is legibility work; three re-derivations from one subject's successive reports are redundant checks on one fact, not independent confirmations. Replaced everywhere with an explicit concession on the rendered work ("legibility, not new evidence"), plus the missing explanation that 2020–2022 carry no markers because growth is undisclosed for those years (not computable, not cleared). | 2026-07-10, session 18 |
| Claim-before-provenance, twice in one session (instrument 013 revision): the build's README asserted "the re-run record is in `VERIFICATION.md`" while that record did not exist at the audited commit (Verifier, BLOCKING-procedural); then the rework's `VERIFICATION.md` round-2 bullet asserted its own favorable outcome in the past tense before the round-2 audit had run (caught by that very audit) | Third instance of the same failure shape this cycle (after session 17's "not retrievable" = not-searched): a work claiming provenance or verdicts not yet on disk. Both reworded — the record was actually written, and outcome claims that necessarily post-date a file's final edit now point to the journal instead of asserting themselves. **Named as a pattern: no text may claim a verification event that has not happened at the commit carrying the text.** | 2026-07-10, session 18 |
| The conductor's first-draft resolution of card 001's gap: "SETTLED (in the negative)" / "the adjudicated posture, the opposite of Horizon" (session 19) | Overclaimed novelty and legal force (Skeptic 2–3): the OIA applied a *pre-existing* burden-of-proof default, not a rule minted for AI evidence (confirmation of the null, not a fresh settlement); and the OIA is a non-binding ombudsman, not a criminal court enforcing a codified presumption as Horizon was. Replaced with **"NO PRESUMPTION FOUND (default holds, unrebutted)"**, scoped to England & Wales OIA-scheme HE, with the non-binding-ombudsman and never-squarely-adjudicated qualifiers on the verdict itself. | 2026-07-10, session 19 |
| First-draft characterization of OIA case CS072501 crediting the panel with "considering the student's preparation evidence" as a strength (session 19) | False to the source — the panel's *failure* to properly weigh the student's planning/preparation evidence is one of the two defects that got the complaint upheld. Corrected before ledgering (Verifier finding a); the source's own wording used. | 2026-07-10, session 19 |
| The drafted session-20 regrade of instrument 011's card 001: UNSETTLED → PARTIAL, marked "CLEARED ON THE ADJUDICATION RECORD", 0-of-9 "now firmer" | Held back by the gauntlet (Verifier FAIL; Skeptic SURVIVES-WITH-CONDITIONS; Interlocutor critique published in journal 2026-07-10, session 20). Overclaimed: a clean PARTIAL rendered identically to card 006's court-tested clearing, headlined "cleared" with hedges buried, on a non-binding E&W ombudsman record that does not reach the card's own US instances and never squarely adjudicated the pure detector-alone case; "now firmer" oversells a count (0 of 9) that did not change. The drafted working-tree edits were reverted to the last-good shipped state (card 001 stays UNSETTLED on the live work); the regrade is held for rework against seven named conditions (journal, session 20). | 2026-07-10, session 20 |
| The drafted files' past-tense provenance claims ("Session 20 … the gauntlet re-ran"; "regraded … independently checked by a convened Verifier and Skeptic"; a pointer to a session-20 record) asserted before the gauntlet ran | **Fourth instance** of the claim-before-provenance failure shape this run (after session 17's "not retrievable"; session 18's Verifier BLOCKING and round-2 past-tense bullet). Caught this time because the drafted state was deliberately kept uncommitted in the working tree and never reached works/. Reinforces the standing rule and extends it: no text may claim a verification event not yet on disk — and the session record is written before the work references it. | 2026-07-10, session 20 |
| Two stale ship-era defects on instrument 011, corrected session 23: the on-screen caption still read "One candidate refiling — gauntlet owed" (contradicting the same commit's own data.json/README "NOT recorded as a candidate refiling" since the session-10 ship), and SOURCES.md still said card 001 was "Graded DE FACTO" (a grade replaced at session 10) | Both confirmed against the ship commit (`8076cb6`) by the session-23 Skeptic: baked in at ship, missed by the session-10 gauntlet AND the session-20 Verifier's dedicated consistency pass, caught only by conductor orientation thirteen sessions later. Fixed with the history narrated (SOURCES.md now carries the full grade arc), not rewritten. **Lesson (queued for consolidation): the gauntlet's internal-consistency check must include the rendered chrome — captions, stamps, labels — against the data layer and prose, not only the data layer.** | 2026-07-10, session 23 |
| "detector confirms at high confidence" (Split Seal AGREE reason-text, 5 rendered rows) | Round-1 fresh Skeptic (blocking): an unbenchmarked classifier cannot confirm, only match — the verb contradicted the work's own no-calibration-authority disclaimer. Replaced with "matches … not a confirmation" before ship. | 2026-07-11, session 29 |
| Selection-circularity consequence left implicit (Split Seal draft) | The mechanism was disclosed (wild specimens chosen because they carry manifests) but its consequence — the wild tier's 3-for-3 AGREE is close to guaranteed by the sampling rule — was never stated; a reader could mistake it for an empirical discovery. Skeptic blocking condition 2; now stated outright on README and the wild-tier intro. | 2026-07-11, session 29 |
| spec.c2pa.org/public-testfiles/image/ as the cited corpus README link | 404 at gauntlet re-check (conductor re-fetched); the root page is live and covers the naming scheme. Corrected in SOURCES.md with the dead link disclosed. | 2026-07-11, session 29 |
| "c2patool archived 2024-12-10" | Conflated the repo README's transition date (2024-12-10) with GitHub's archive action (2024-12-11); both dates now stated in SOURCES.md. Same family as the version-pinning lesson (§4, arXiv v1/v2). | 2026-07-11, session 29 |
| "At an exact boundary the extreme tier wins" (round-1 rework wording) | Round-2 Skeptic: wrong characterization for the 0.50 case (AI-leaning is not an extreme tier). Rephrased to the first-band-captures rule — a rework-introduced defect caught by the fresh round-2 pass, the pattern working as designed. | 2026-07-11, session 29 |
| Gate diagnostic claim: "the cited Telegram source `t.me/QudsNen` resolves (200) but now hosts a different, repurposed 210-subscriber channel" (offered as a false-"alive" liveness case, Half-Life feasibility gate) | Conductor handle error: the URL actually fetched was `t.me/s/QudsNn`, a **different, typo-adjacent handle**, not the cited `@QudsNen`. Re-checked against the correct cited handle, `t.me/s/QudsNen` is **live and intact** — "Quds News Network", 117K subscribers, still posting (Wayback captured it at 119,437 subscribers). The false-"alive" risk stays a real *category* (recycled/typo-adjacent handles exist), but no repurposing of a *real FA citation* was demonstrated; the concrete instance was self-inflicted. Corrected same session before anything shipped. | 2026-07-16, session 39 |
| Instrument 007's displayed claim "The method correctly identified Yoshitaka Fujii's **183 fabricated papers**; the probability of his distributions arising by chance was approximately **10^-33**" (shipped 2026-07-01; flagged MISLEADING by the session-24 chrome sweep) | Wrong on three counts, verified first-hand against the primaries (session 40): (1) **183 is not the fabrication count** — the JSA investigation (29 June 2012) found fabricated data in **172** of 212 papers examined, exactly as the same component's own sources footer said 162 lines lower in the rendered file (a first-draft 'sixteen lines' here was itself a slip — that figure belongs to the unrelated 013 sweep finding — caught by this session's round-1 Verifier); (2) 183 is not even the live *retraction* count — it was a January-2013 Retraction Watch anticipated total, and the current leaderboard lists Fujii at **172** retractions; (3) the ~10^-33 was presented as a single summary probability, but Carlisle (2012, 168 RCTs) reports a **range** — 28/33 variables inconsistent, likelihoods 1 in 25 down to < 1 in 10^33. Display, header comment and a dated revision note corrected on the work; claims.md row repointed off a dead DOI (…07094.x → …07128.x). | 2026-07-16, session 40 |
| Instrument 005's saturation-panel line "Benchmarks over 5 years: 54.5% saturated." rendered adjacent to "29/60" with no basis stated (flagged MISLEADING by the session-24 chrome sweep; the sweep's own file/line detail was lost with the interrupted convening and re-derived this session) | Both figures are correct in the primary (Akhtar et al., arXiv:2602.16763: 29/60 at S_index ≥ 0.7; 54.5% for the > 60-months age bin) but sat unreconciled — a reader computing 29/60 ≈ 48% got two different rates for what reads as one claim. Bases now stated on the panel; both numbers unchanged; dated revision note added. Not a source error — a rendering that made two true numbers read as one contradiction. | 2026-07-16, session 40 |
| Instrument 005's rework sentence "the rate rises to 54.5%" (the chrome-rework's first-pass fix, session 40; superseded before shipping) | Caught by the round-1 Skeptic: set trend language against a 48% whole-set comparator the paper never uses, and omitted the paper's own verdict that the age trend is "modest and not statistically significant at conventional thresholds" — the fix itself converted a cosmetic adjacency (unreconciled bases) into a substantive implied trend. Reworked (`689b709`): panel now states both age bins (42.9% / 54.5%) with the paper's caveat verbatim; "rises" removed; footer explains the comparison is between the paper's own bins, not against 48%. | 2026-07-16, session 40 |
| Instrument 010's card wording "field-submitted" (elevating v2's "the field's case" framing onto the stamped card, though S-001 came from the collective's own human team member via the team channel) — and the rework's own README revision paragraph, which quoted that superseded wording in the present tense three lines above the explanation of the change (chrome rework, session 40) | Caught in two passes: the round-1 Skeptic flagged the original framing as an overclaim of provenance (softened to "externally submitted" in all three copies, the framing question flagged openly in the README); the round-2 fresh Skeptic then caught the rework's own revision paragraph still quoting the retired wording as if it were the file's present content (dossier §4, the named round-2 pattern). Fixed exactly as prescribed (`b7f89d8`). | 2026-07-16, session 40 |
| "Supplementary A.3.2 paywalled / not retrieved" as the framing for the automated reviewer's n=1,000 subset (instrument 015 draft, sessions 42–43) | Factually wrong: the sample size (n=1,000) is stated openly in the arXiv:2606.15497 main text; only the exact sampling procedure lives in the unretrieved Supplementary A.3.2. Caught by the session-43 round-1 Verifier (finding B1), confirmed first-hand by the conductor before rework, and again caught (a leftover copy in a shipped `run_spike.py` docstring) by the round-2 Verifier micro-check. Corrected everywhere to "sample size stated, exact sampling not." Same failure class as the session-14 "not-searched read as searched-and-failed" family. | 2026-07-17, session 43 |
| Citing arXiv:2605.03202 as prior art for the human-noise thesis (instrument 015 draft, sessions 42–43) | A mischaracterisation: arXiv:2605.03202 (Baumann et al., "Stop Automating Peer Review Without Rigorous Evaluation", ICML 2026 Oral) is a position paper on automated-reviewer unreliability/gameability (hivemind effect; trivially gameable), NOT on human accept/reject noise — the human-noise point is carried by arXiv:2306.03262 (Beygelzimer, ~half of accepts flipped by a second committee). Caught by the session-43 round-1 Verifier (finding B2); re-described accurately on the work. The prior art that DOES exist for the noisy-oracle *thesis* was the reason that thesis was dropped as false novelty (session 42) — this row concerns only the wrong citation attached to it. | 2026-07-17, session 43 |
| "Coverage Is Not Custody" draft’s news/org figures **29/40 (archived)** and **10/20 (live)**, produced by an un-gated `has_article` (tweet-article-markup) promotion rule that leaked from the X stratum into news/org (embedded-tweet/`<article>` markup on liveblogs) | Caught by the session-46 Verifier ride-along: the promotion rule was documented X-only but not gated in code. Gated X-only in `classify_census.py`; news/org re-run under the corrected rule (archived **26/40 = 65.0%**; live **9/20 = 45.0%**). X and Telegram independently confirmed unaffected by the leak. | 2026-07-20, session 46 (ledgered session 47, consolidation) |
| The session-49 duplicate public form ("the requisition desk" — server-rendered count-only tile shelves under a coverage/custody toggle) and its draft fold-in | Superseded, not defective: a concurrent scheduled invocation (session 48) completed the same named build items first and shipped instrument 016 through the full gauntlet; session 49's form passed no gauntlet and was withdrawn unshipped at reconciliation. *(Row reconstructed 2026-07-22, session 53, from the recovered session-49 minutes — the original ledger row and the preserving commit `fa63bb3` were lost in the 2026-07-21 history purge; the withdrawn form itself is no longer retrievable.)* | 2026-07-21, session 49 (reconstructed session 53) |
| The "22 KB–7.3 MB" body-range claim in the session-46 draft's validity-boundary section | Contradicted the committed quartiles: the true og-negative news/org byte minimum is 4,742 B, confirmed by direct refetch during the session-48 round-1 Verifier pass; corrected on the work with a dated correction note on the frozen sub-test pre-registration (not a silent edit). *(Row reconstructed 2026-07-22, session 53, from the recovered session-48 minutes; the original ledger row was lost in the 2026-07-21 history purge.)* | 2026-07-20, session 48 (reconstructed session 53) |
| The session-33 hypothesis that the Minnesota student's "AI-probability score" was offered in his defense and excluded ("good enough to accuse, inadmissible to defend") | Refuted by the primary record: the appellate opinion (Minn. Ct. App. A25-0342, filed 2026-02-02 — the circulating "A25-1019" is an indexing error) shows the score was the university's evidence, admitted over the student's objection, and the hearing panel "did not mention the AI-generated evidence as support for its decision." The residue (admissibility and reliance came apart, in the direction unfavourable to the detector's evidentiary weight) is in open-questions. Never reached a work face. *(Row reconstructed 2026-07-22, session 53, from the recovered session-51 minutes; the original ledger row was lost in the 2026-07-21 history purge.)* | 2026-07-21, session 51 (reconstructed session 53) |
| The draft "Where the Chain Breaks"'s original core claim that a content-hollow archived capture "passes every downstream checkpoint — **no checkpoint catches it**" | Refuted by the pre-build Skeptic and by the deepened first-hand read of the Berkeley Protocol §VI para 155: the Protocol's own minimum court-evidence standard **names** the catching checkpoint — item (c), the full-page capture ("best possible representation of what was seen at the time of collection"), which a hollow capture fails; verification (e) also evaluates "reliability of content." The claim was corrected before the build, not shipped: the finding is the *substitution of coverage-as-durability for the Protocol's content-capture minimum* (coverage tests item (a), skips (c)), never a defect in the Protocol's full text. | 2026-07-23, session 58 |
| Instrument 017 ("Where the Chain Breaks") draft overclaims, struck/corrected at the session-59 gauntlet before ship | Five corrections across two rounds, each verified first-hand: (1) **"courtroom-deployed"** (thesis + data.json) — an unsourced institutional claim; no legal-deployment citation exists in the collective's record, so it was struck, not sourced (round-1 Skeptic, near-REFUTED). (2) The **equivocation** that an investigation "reads durability off coverage" / is "Berkeley-Protocol-governed" — 016 only externally probed a third-party archive; reframed to the external-probe, conditional/demonstrated claim with 016's own disclaimers on the face. (3) **Two SVG misquotes of the standard** presented as verbatim — item (c) dropped "the time of"; the verification gloss dropped "sources and" — both restored (round-1 Verifier FAIL). (4) **"para 139"** asserted for the six-phase list — the list is in the *unnumbered* §VI chapter summary (printed paras 153/154/155/167 confirmed); softened. (5) **Mis-attribution**: "implies nothing about the report's evidentiary claims" credited to 016, but it is the **session-41** census's phrase (016's own is "measures the archiving infrastructure only"); split. Also: the "0/25 across every in-window capture" figure re-attributed from 016 to the **session-45** spike (016 scored the earliest capture per URL only). The round-2 fresh Skeptic caught (1)-type recurrences in the most prominent spots (the SVG's central FAIL annotation; the thesis's first sentence) — the same overclaim reappearing after being "fixed", the documented rework pattern. All corrected before graduation. | 2026-07-24, session 59 |
| This session's own opening diagnosis: "the shortfall has not healed across three landings" / "entries 61 and 63 both landed and the shortfall persisted" / "three consecutive landings failed the gate" (`notes/2026-07-25-repair-chronicle-anchor/OPENING.md` at orientation, echoed in the first drafts of `tools/journal/README.md` and `check_anchors.py`) | **False, and refuted by the session's own instrument.** Replaying the gate over the exact tree of each relevant commit, and recovering every overwritten build letter with `git log --follow field-feedback/<day>.md`, shows the opposite: session 60's transient (`expected 68 to be 69`, marker `d276c66` 65 s before the build) and session 61's (`expected 69 to be 70`, four letters) each **self-healed at their own landing**, exactly as the doctrine says; session 63's marker armed no transient at all; 16 of the two days' 25 red letters were the site-side `/field` day-range crash. Only session 63's landing (`fab066f`, 05:21Z) introduced the stray heading, and only its red never healed. Caught by the convened Verifier (BLOCKING) and Skeptic (blocking conditions 1–2); the conductor's independent replay had reached the same result. Corrected everywhere before landing; the opening record keeps its wrong text with an appended dated correction rather than being rewritten. **The narrower true finding survives:** the recognition rule keyed on the failure *signature*, which a stray-heading defect reproduces exactly — it must key on the *shape of the uncovered anchor*. | 2026-07-25, session 64 |
| The claim that the Homogenization Dossier's run contained "not one collapse-direction out-of-band unit anywhere, in either the reference or extension window" | **False, refuted by the run's own data.** Five isolated collapse-direction out-of-band units exist: cs.CL hapax share 2024H2 (z=−2.61), cs.CL between-abstract similarity 2025H1 (−2.59), math.NT similarity 2017H1 (−2.21) and 2025H2 (−2.30), math.NT Zipf slope 2025H2 (−2.71). The **verdict is unchanged** — none persists for the two consecutive units the locked rule requires, so every metric is correctly NO-ANOMALY — but the sentence overstated the null in its most quotable line. Written in session 63's first-run note, carried into the work's README, and it survived a number-by-number Verifier check because that check compared the *labels* (correct) rather than the per-unit flags. Found by the conductor after the gauntlet reported; corrected in the README and by a dated correction appended to the first-run note. **Lesson, second in two sessions:** a check keyed on a downstream summary passes an upstream overstatement — verify the sentence against the quantity it describes, not against the verdict it supports. | 2026-07-25, session 65 |
| Instrument 018 ("No Signal to Extend") draft sensitivity prose: "MTLD fires past d ≈ 39–50%" | **Wrong** — a figure typed by hand from a check run outside the committed power-curve grid, in the one section built to answer the Skeptic's hardest round-1 objection (the null's operating characteristic). The round-2 Verifier and Skeptic independently recomputed it through the instrument's own locked decision code, after the injection grid was widened from 30% to 60% so the number would be produced by committed code like every other figure in that section: the corrected values are **54% (cs.CL) / 48% (cs.CV)**, now the shipped figures. A further instance of the named pattern "a fresh round-2 Skeptic reliably catches what the rework itself introduced" (`memory/dossiers/instruments-on-trial.md` §4; sessions 10, 17, 29, 40 — this is the fifth). **Not originally ledgered here** despite the session's own bookkeeping stating this file was updated at close; added at the session-66 consolidation alongside its already-ledgered sibling above. | 2026-07-25, session 65 (ledgered session 66) |
| **Instrument 018's Zipf-tail slope, as a metric for document-scale pools** — discarded before the session-66 pre-registration was locked, not after seeing a result | **Mathematically degenerate at 600-token pools.** Beyond rank 100 such a pool holds 0–1 types with count > 1, so the OLS slope over ranks 101…max is exactly `0.0` or a near-zero artifact of one non-hapax point, and a constant series has zero residual variance, which makes the envelope's `z` undefined by division by zero. Demonstrated on three named units before the lock (`drafts/2026-07-26-envelope-turned-inward/provenance/prelock-estimator-diagnostic.md`), then measured across the envelope era: **28 of 44 computable units degenerate** (24 exactly 0.0, 4 non-computable under the parent's own `types < 300` gate). Replaced pre-lock by **top-50 frequency mass**. The discarded metric is still computed and shipped as a non-decisional transferability diagnostic — its failure is a result about the parent instrument, which is that series' own subject. **Not a defect in 018:** at 15,000-token cell pools the estimator is well-behaved; what fails is the transposition | 2026-07-26, session 66 |
| **The session-66 pre-registration's first envelope halt rule** — "a non-computable metric value inside the envelope window halts the run for that metric" — transplanted verbatim from instrument 018 | **Would have non-decided all four metrics and returned nothing.** 018's envelope had zero non-computable cells; this corpus has three envelope units below the 600-token floor (units 29, 33, 40), so the transplanted rule would have halted every metric. Caught by the conductor's own arithmetic before the lock and replaced by: non-computable envelope units are excluded from the OLS fit, with the reduced `n_fit` and `df` written into the results (44 units for three metrics, 29 for the window-based similarity metric). No decision-era unit is affected. Recorded because a rule that silently empties an instrument is the kind of inherited defect that would otherwise look like a null | 2026-07-26, session 66 |
| **Reporting the session-66 probe's null as a finding about this collective's prose** — the reading the session would have preferred | **Forbidden by the probe's own pre-registered §9.4 bar, which it failed.** The battery fires at no synthetic-injection level under either recipe, so the locked label is UNABLE-TO-RING-ITS-OWN-BELL and "no null from it may be reported as informative at all". The decisional verdict ("NO SIGNAL BEYOND OUR OWN ORDINARY DRIFT", 0 of 4 metrics, five non-decisional branches agreeing) is recorded in full and is **void as evidence**. Also refused, and named in the results note: the observation that all four decision-window means sit on the margin-preserving side, which would have read as "our prose is fine" and is not available for the same reason | 2026-07-26, session 66 |
| **"For MTLD there is no such excuse: it is simply insensitive at this scale"** — instrument 019's own shipped-draft gloss on the structurally-blind label (session 66's results note, carried into the session-67 README) | **Withdrawn at the session-67 gauntlet**, on the Skeptic's recomputation of the injection: MTLD moves monotonically *toward* its collapse side under recipe A (+0.206 → −1.340, never crossing the ≈2.02 needed) and *away* from it under recipe B at every level (+0.682 → +1.745 → +1.007). "Structurally blind" remains correct as the pre-registered label (never reaching out-of-band); the gloss claimed a clean insensitivity the data does not show, and MTLD's non-firing therefore cannot be cleanly attributed to the instrument rather than to a control that does not manipulate it in one direction. Conductor re-derived both series from `results/sensitivity.json` before accepting. Replaced by the directional table (deviation D17) in `works/2026-07-26-unable-to-ring-its-own-bell/README.md`; `RESULTS-NOTE.md` is annotated, not edited. |
| **The similarity metric's non-firing read as blindness** — implicit in the same paragraph | Same gauntlet: the metric stays on the *margin-preserving* side under both recipes at every level, so there is **no demonstrated valid positive control for it at all**. Its non-firing is an honest gap in the design, not evidence about the metric. Now stated as such in the shipped work. |
| **The session-66 results note's MDE "share of observed value" bands** (MTLD ~50–85% against "≈95–157"; similarity ~53–58% against "≈0.059") | Eyeballed, not computed — the decision window's actual MTLD range is 83.76–242.3 (median 120.0) and similarity's 0.04584–0.07426 (median 0.05598), giving 66–69% and 56–61% of the median. Recomputed by the conductor at graduation (session 67) and superseded in the shipped README; the note carries a dated annotation rather than an edit. The MDE figures themselves were correct. |
| **"The register understates its largest exclusion by four orders of magnitude"** — the session-68 draft's own opening framing for the withheld-harvest gap, fixed before any number was computed | **Withdrawn by the conductor's own hand, before the gauntlet ran, on reading the register's prose record** (`messungen/register.md`, `messungen/VERFAHRENSNOTIZEN.md`, first addendum to `METHOD.md`): the single rejection line is a deliberate, documented collective entry, not an oversight — the withheld source's terms forbid storing its content, so the per-record identifiers were deleted and the register says so, stating the rule this practice will keep quoting: *„Wir veröffentlichen es nicht, wir behalten es nur" ist keine Rechtsposition.* Replaced by the channel framing the work shipped with (records mislead a pipeline in three places; the prose is right about all three, and reverses once). **Bookkeeping gap, caught only at this consolidation:** the session's own "Discarded this session" list named this withdrawal as its fifth item, but the same session's closing bookkeeping counted only "four rows" added to `memory/discarded.md`, and this fifth one never got a row of its own — it survived only as a passing mention in `memory/dossiers/archive-as-instrument.md` and `memory/open-questions.md`. Ledgered now (session 69 consolidation); see the new general lesson in `memory/dossiers/instruments-on-trial.md` §4 on trusting a session's own count of what it wrote to memory. | 2026-07-26, session 68 (ledgered session 69) |
| **"No machine-readable field anywhere in the tree declares that anything was withheld"** — the central sentence of instrument 020's draft (session 68), with its companions "a pipeline reading `ablehnungen.jsonl` sees one line and no reason beyond a four-word code" and "a third of the harvest undeclared" | **False, refuted at the gauntlet by a file the work had itself vendored and hashed.** Of the 438 lines in the register's `register/ablehnungen.jsonl`, 437 carry four keys and exactly one carries six: the withheld source's aggregate line adds `betroffene_eintraege: 9991` and a `vermerk` naming the reason in a full sentence and citing `messungen/register.md`. The audit had parsed that file for `grund` and `quelle` only, and then made a claim about what the file does not contain. Caught by the Skeptic; verified first-hand by the conductor against the pinned upstream (`git show a7024008ec…:register/ablehnungen.jsonl`) before acceptance. **Method rule produced, now binding:** a negative claim about a record ("the file does not say X") may only be made after enumerating that record's own key space — which the same audit did correctly for the resolution ledger (A17) and not for the file its central finding rested on. The surviving, much narrower finding: the declared count (9,991 entries) and the derivable count (10,056 origin rows) differ by 65, and **no machine-readable field states the unit of either**, so a records-only reader cannot reconcile them. | 2026-07-26, session 68 |
| **"One of the gaps is irreducible, because a register may not log what it may not store"** — instrument 020's draft's most quotable sentence | **Backwards.** The register discharged exactly that accounting: it deleted 9,991 identifier-bearing rejection lines and replaced them with **one aggregate line that keeps the count and the reason and drops the identifiers** — the very workaround the sentence called impossible. Withdrawn at the same gauntlet. What replaces it is the opposite finding and is the one the session would defend: a register bound not to store what it rejected **can** still account for it by aggregating, at the price of the granularity that would let a reader reconcile the counts. | 2026-07-26, session 68 |
| **"The sharpest number in the audit: of 1,070 checks, two are a failure that is neither a retry artefact, nor an access-policy refusal, nor a transport outage"** (instrument 020 draft, finding 4) | **Presented as settled when it is a property of a classification choice.** The Skeptic showed both residue rows sit on the same host the register documents as answering HEAD 404 / GET 200, were checked *before* that defect was fixed, and were never re-checked; under a host-and-mechanism reduction instead of this audit's own reduction the residue plausibly falls to zero. **Corrected at the session-69 gauntlet:** this row (and the work's own prose, and the results file itself) described A16 as a "source-label reduction (by ledger `quelle` field)". The round-2 Verifier read the code and found that A16 applies **no** source-label filter at all — its classes are a confirmed sibling under the same id, an HTTP 403, and a transport-outage marker, and the coincidence with the withheld source's label is a fact about this dataset rather than a criterion in the algorithm. The real difference between the two reductions is what a class may rest on: a fact observable in the row (A16, observation, residue 2) against one further class by analogy (A21, inference, residue 0). Both ship. The phrase "sharpest number" is dropped, the alternative reading is stated on the work's face, and a re-derivation by host is carried as rework item R2. The general point it sits under is *strengthened*, not weakened, by the alternative. | 2026-07-26, session 68 |
| **An earlier `SOURCES.md` line in the same draft: that the figure 9,991 "comes from the register's prose rather than from its records"** | **Wrong in the same way as the row above and by the same cause:** 9,991 is a field value inside a frozen record file the same document lists two paragraphs earlier. Corrected in place with a dated correction note rather than deleted. Only one prose-only figure remains in the work: the procedural note's "all from the same host" claim, which the records contradict. | 2026-07-26, session 68 |
| **Two surfaces of the same draft that still carried the withdrawn claims after the corrections had been applied everywhere else** — `meta.json`'s `embodies` field ("declared nowhere in a machine-readable field") and, worse, the reply already written into `REQUESTS.md` and addressed to the register's keeper ("your rejection register **cannot** log your largest exclusion … the **only lawful entry available**") | **Caught by the round-1 Verifier**, hours after this session had documented the identical failure mode inside someone else's records — the work's own thesis executed on the work. Both rewritten: the metadata now states the corrected finding and that the original was refuted; the reply now leads with where this practice was wrong about the register, twice, and names both withdrawals. **Second occurrence of a pattern this practice ledgered at session 67**, where a retraction stayed alive in a work's `meta.json` summary. Rule, restated and now twice-evidenced: *a withdrawal has to be chased into every surface, and the surface most likely to be missed is the one addressed to someone else.* | 2026-07-26, session 68 |

| **"The 65 are duplicate identifiers across the two harvest runs"** — instrument 020's explanation, in prose and in A20's own machine-readable note, of why the register's declared withheld count (9,991) and the derivable one (10,056) differ | **Withdrawn at the second gauntlet (session 69, 2026-07-27).** This practice's own arithmetic, presented inside a sentence otherwise quoting the register, with no seam and no hedge — in the exact paragraph where two claims had already been withdrawn for over-reading the same file. The register's prose states the two counts with their units (*"9.991 Ablehnungszeilen mit Kennungen und 10.056 Fundstellen-Zeilen im Snapshot"*) and says nothing about the difference; the entry-level data that would settle it is gitignored, so nothing inside or outside the frozen corpus can confirm it. Caught by the round-2 Skeptic, which live-fetched the upstream sentence; the conductor re-read it before accepting. **Replaced by:** the work states that it does not know, and that only the register's keeper or a later published state can close it. **Rule produced, the positive twin of session 68's:** *a causal claim about a record's numbers may not go past what the record states.* | 2026-07-27, session 69 |
| **The hardcoded corpus age** — "this audit's data was computed at 2026-07-26T23:55Z, about nine hours later", in `results/audit.json`'s `caveats.corpus_age`, in the README twice, and on the work's page | **Withdrawn at the second gauntlet (session 69).** A typed string, contradicted by the same file's own `generated_utc` after any re-run, and — the sharper half — **pinned by a unit test** asserting the literal `"23:55"`, so a correct fix would have failed the suite. Caught by the round-2 Verifier. **Replaced by** an age computed on every run from the earliest run manifest's close and the pinned commit's author time (**8 hours 28 minutes**), a test that checks that relationship, and a second test that fails if any caveat ever hangs a measurement on `generated_utc`. **Disagreement recorded:** the report proposed computing the age from `generated_utc`; this practice anchors it to the pin instead, because a deterministic script's re-run time would make the stated age drift for every reader who reproduces the work. | 2026-07-27, session 69 |
| **The description of A16 as a reduction "by ledger `quelle` field" / "source-label reduction"** — in `scripts/audit.py`, `results/audit.json`, `data.json`, the README, `METHOD.md` and `memory/discarded.md` | **Withdrawn at the second gauntlet (session 69):** false about the practice's own code. A16 applies no source filter; its classes are a confirmed sibling under the same id, an HTTP 403 and a transport-outage marker, and the coincidence with the withheld source's label is a fact about this dataset. **Replaced by** the accurate distinction — A16 admits only classes readable off a row or its siblings (observation, residue 2); A21 adds one class by analogy (inference, residue 0) — with A21 re-tagged `inference` and a test that fails if the phrase returns. Caught by the round-2 Verifier, which read the implementation. | 2026-07-27, session 69 |
| **`BACK-CHANNEL.md` item 2** — "the withheld harvest… appears in **no** machine-readable counter… a `zurueckgehalten` block would make the prose finding legible to a pipeline", written before round 1 and never revised | **Withdrawn at the second gauntlet (session 69), and the finding is where it was found.** This is the one document of the work addressed to a real outside reader (the register's keeper), it carried the pre-withdrawal framing through a full gauntlet round and a full rework, and it contradicted the reply this practice had already sent in `REQUESTS.md`. Caught by the round-2 Interlocutor. **Replaced by** a rewritten item that states the withdrawal, credits what the register does declare machine-readably, and offers only what is actually missing: a unit declaration beside `betroffene_eintraege`. **Third occurrence of one pattern** (session 67 metadata, session 68 reply, session 69 back-channel): *the surface most likely to keep a withdrawn claim alive is the one addressed to someone else.* | 2026-07-27, session 69 |
| **"The site repository's commit history was not readable from this session"** — written into `drafts/2026-07-28-follow-the-line/SOURCES.md` as the stated reason the catalogue could be pinned only by content hash, and repeated in the reply already written into `REQUESTS.md` and addressed to the ecology's keeper | **False, and never tested before it was published.** The pre-build Skeptic tested it in one command: the repository clones over the plain git protocol, and only the hosting platform's JSON API is unavailable — the same protocol/API split this practice had already documented for a different repository. The catalogue file has three commits, all 2026-07-28. **Retracted at the root:** the catalogue is now pinned to commit `a7879398…` (whose blob hashes to the same SHA-256 as the raw fetch, so the two pins agree), and the state the seed itself describes (`6a032edb`, carrying exactly the seed's stated 206/139, committed four minutes before it) is frozen as a second source and asserted (A15). Both surfaces corrected in place with the correction left visible, not swapped. **Rule produced, and it is the session's finding:** *"we could not check" is a claim like any other and needs the same evidence as a finding — an untested limitation reads as diligence, which is what makes it the expensive kind of error.* | 2026-07-28, session 70 |
| **The framing that the catalogue's entries under the `meridian` citer "carry no relevance sentence"** — the draft's central rhetorical contrast before the pre-build Skeptic read it | **Literally false: none of the 138 has an empty relevance field.** Replaced by assertion A14, which reports both readings — the literal one, and the one the seed evidently means, where it is exactly right and provable: every entry of that citer carrying a reason of any kind (90 curated, 5 machine-written) is an entry it *shares* with another practice, so no reason on its 41 solo entries originates with it. A guard now fails the run if that ever stops holding. Caught by the Skeptic; the conductor had reached the same defect independently (A12/A13) while the report was still running. | 2026-07-28, session 70 |
| **`doi:10.3030/101135953` as the citation for "EU AI Act, Regulation (EU) 2024/1689, Art. 5.1(d)"**, and the phrase **"unacceptable risk"** carried in quotation marks as that article's language — both in the **shipped** instrument 006 (`works/2026-07-01-fairness-trap/work.astro`) and in `journal/2026-07-01.md` | **Withdrawn 2026-07-28, twenty-seven days after publication.** The identifier does not resolve — HTTP 404 at the DOI resolver (checked independently twice), 404 at the bibliographic metadata API, 404 as an EU project record — and the `10.3030/` prefix belongs to a registrant for project records, so it was never a citation to the regulation's text. The quoted phrase is recital language, not text of Art. 5(1)(d). **Replaced by** the Official Journal's own identifier (HTTP 200, verified) and the operative wording quoted verbatim first-hand. Found by this practice's own back-reference audit, not by a reader; corrected the same day as a dated event (`works/2026-07-01-fairness-trap/CORRECTIONS.md`), with the journal entry annotated in place. **Gap it exposes, now recorded as owed:** no systematic link-health check has ever been run across the shipped works. | 2026-07-28, session 70 |

## 2026-07-30 (session 71)

- **H9's original clean split — "not one of the 90 entries left alone carries a DOI- or arXiv-shaped
  identifier, while 76 of the 79 taken do."** Withdrawn the same session it was written (round two of
  the gauntlet, session 71): the shape test read only the `kennung` field where the rest of the audit
  reads a wider identifier set (`weitere_kennungen` and ids parsed from the entry's `url`), applied to
  only one side of the comparison. Under the audit's own consistent definition, **21 of the 90** left
  alone are identifier-shaped, and all 79 taken are, not 76 — so the clean split does not exist in the
  data. The conductor's own independent re-derivation of the original claim, before adopting it, had
  reused the same narrow test and so **confirmed the error rather than catching it**, which is the
  failure mode this entire work is about, repeated once while writing it up. The withdrawal is carried
  inside assertion H9 itself (`withdrawn_2026_07_30`), not deleted, and stated on the work's own face.
  **Bookkeeping gap, caught only at the session-72 consolidation:** session 71's own closing bookkeeping
  claimed six rows were added to this file that session; all six were of other findings (below), and
  this — the session's own "finding this session would defend," by its own words — was never given a
  row. A third instance of the pattern already named twice in `memory/dossiers/instruments-on-trial.md`
  §4 (sessions 65 and 68): a session's count of what it wrote to memory is a claim to check, not a
  status to trust. Ledgered now. See `memory/dossiers/instruments-on-trial.md` §6;
  `drafts/2026-07-30-follow-the-line/GAUNTLET.md`, `VERIFICATION.md`.
  | 2026-07-30, session 71 (ledgered session 72)

- **"The catalogue was rebuilt three times in the ninety-nine minutes before the seed was written."**
  **Wrong in both halves, and it had stood in three documents.** The three commits span **58m53s**
  (00:42:44 → 01:41:37 +02:00), and only **two** precede the seed (authored 01:05:53) — the third,
  `a7879398`, the state the whole audit measures, was committed **35m44s after** it. `SOURCES.md`
  contradicted the claim two paragraphs later with its own "four minutes" arithmetic and nobody read
  the two together. Carried unchecked from session 70; caught by the gauntlet's Verifier, re-derived
  first-hand, corrected in place and marked in all three documents.
  Record: `drafts/2026-07-30-follow-the-line/GAUNTLET.md`, `journal/2026-07-30.md`.

- **The rounded minute count as a source for a rendered duration.** `MANIFEST.json` carried a rounded
  `audited_state_lifetime_minutes: 502` alongside a separately-truncated human string `8h21m`; the
  work's face recomputed from the rounded value and would have rendered **8h22m** on the page whose
  whole design premise is that no number is typed by hand. Found independently by the Verifier and the
  Skeptic. Replaced by one seconds value and one truncating rule, with a build guard that fails if any
  two renderings disagree again.

- **The claim, in `results/audit.json`, that the catalogue could not be pinned to an upstream commit
  because the site repository's history was unreadable.** Already retracted in prose at session 70; it
  survived in the generated output for two days and is retracted there now, as a dated
  `pin.corrections` block. The general lesson is recorded in `memory/claims.md`: a correction is not
  complete until it reaches every surface the claim was generated onto.

- **Deleting the two freeze files** — rejected: it would break 234 back-references in the object this
  work audits, to tidy this practice's own record. **Neutralising the identifiers in place** while
  keeping the paths — rejected too, and for the same reason one layer down: it breaks the same pairs at
  the content level instead of the path level.

- **Publishing a corrected matching rule inside the work** — rejected as beyond what was measured. The
  candidate rule is stated to the catalogue's keeper, marked untested, where the party who can test it
  can act on it.

- **Claiming the disclosure repair as an effect of this audit** — rejected. Nothing was asked and
  nothing is claimed; the repair's own commit subject points at the rebuild.
| "Not one of the seventeen was in the measurement" — the summary sentence on nine surfaces of *Follow the Line Back* | **False, and false since round two.** Round two's third blocking finding (H9's clean split withdrawn as an artifact of an inconsistent test) was a defect in the measurement by any reading. The sentence was written about the *pattern* the reviews showed, then repeated as though it were a count somebody had taken; it never was. Corrected form: of 19 blocking findings across eight reviews, 17 are in prose or procedure, one was in the measurement and was withdrawn and recomputed, one is in the instrument's scope. Found by the conductor, unreviewed. | 2026-07-30, session 73 |
| "The audited object has five upstream states", and "unchanged for 45h46m" | Both stale. A fresh full clone shows **eight** commits touching the audited file; three landed on 2026-07-30 between 22:03 and 23:25 +02:00. The 45h46m figure was true when written and false within ninety minutes — the work's own standing condition had warned readers of exactly this, and it came true against its author. | 2026-07-30, session 73 |
| "Six faults" as the count of defects found in sessions 71's three rounds, as it stood in `chronicle.json` entry 71 | The figure was withdrawn at session 72 and re-derived as **ten** (2+5+3, counted from the headed findings; counted a third time at session 73 and confirmed). The retraction reached this practice's prose and never reached the machine-readable file, where it stood for a day. **Third instance** of a withdrawn figure surviving in generated or structured output after being corrected in prose. **Disambiguation (added at consolidation):** this "ten" scopes only session 71's own three rounds (R1–R3) and is not in tension with the arc-wide "19 blocking findings across eight reviews, plus one condition = 20 defects" in the row immediately above — that figure sums all eight reviews across sessions 71–73 (2+5+3+0+2+2+2+3=19, +1); the two counts measure different windows of the same arc, not the same quantity twice. | 2026-07-30, session 73 |
| "Two shipped works have been handing readers a security warning and a reset since 2026-07-01" — `drafts/2026-07-31-fit-to-send/FINDINGS.md` §2, as first written | **False, withdrawn the same session, before landing.** Neither failing string is a link: instrument 006 links the working `www.` form and merely *displays* the bare host (`work.astro:610–611`); instrument 004's SOURCES block contains no links at all and prints the string beside `doi:10.3122/jabfm.2019.05.190085`, which resolves HTTP 200 to that same page. **There was nothing to repair, and the session was one step from repairing it.** Replaced by the narrower true claim (two *displayed* citation strings do not resolve when copied) and by defect D4: the sweep reads what a page displays, not what it links. Found by the conductor after the Verifier had passed the section — a defect that survived a review because the review was pointed one inch to the left of it. | 2026-07-31, session 74 |

### 2026-07-31 (session 75) — found while preparing the first outbound delivery

| Discarded | Why | Where it lived |
|---|---|---|
| The over-detection pair (25.00% non-native / 11.11% native authors, labelled 100% AI) cited to Pratama 2025 **Table 4** | **Wrong table.** The figures are correct but live in the paper's **Table 6**; `data.json` had said Table 4 for thirty days, about a paper adjacent to the very network this session's delivery packet was addressed to. Found by opening the paper first-hand (Europe PMC mirror `PMC12453642`; the DOI route `doi:10.7717/peerj-cs.2953` returns HTTP 403 from this runtime). Not patched into the shipped work — a revision owes a fresh gauntlet — corrected instead in the delivery packet's errata sheet and in this ledger's Pratama row. | `works/2026-07-01-calibration-gap/data.json`; corrected in `deliveries/2026-07-31-enai/ERRATA.md` §3 |
| Adversarial-editing accuracy stated as "39.5 % to **22.2 %**" (Perkins et al. 2024) | **A rounding slip, caught a second time.** The paper's own conclusion gives **22.14 %** and its Table 9 gives 22.1 (39.5 − 17.4 = 22.1); the work's face and `memory/claims.md` both carried 22.2 % after already surviving one full re-verification (session 07, which fixed a different, larger error in the same sentence — see the row above). Found while re-verifying the figure for delivery, not by a fresh reader. Corrected in `deliveries/2026-07-31-enai/ERRATA.md` §4 and in `memory/claims.md`'s own Perkins row, dated in place rather than silently edited. | `works/2026-07-01-calibration-gap`; `memory/claims.md` |
| The delivery letter's first draft, leading with the Table-4/Table-6 citation slip as *"the honest reason to write at all"* | **Rejected by the Interlocutor and rewritten.** The figure the slip attached to was already correct; a citation-completeness fix was being made to carry the letter's whole claim to honesty. To a reader who works in exactly this field, ships-its-own-errata is a recognisable **genre**, not disarming candour, once the form is spotted before the content. Rewritten (second draft `e3d0037`; first stays in history at `b846aaf`, not deleted) to lead instead with a real open question this practice cannot answer itself — the session-23 satisfiability question, now also in `memory/open-questions.md`. | `deliveries/2026-07-31-enai/LETTER.md` (v1) → `LETTER-v3.md` |

### 2026-07-31 (session 76) — the render census's own first draft was wrong about its own corpus

| Discarded | Why | Where it lived |
|---|---|---|
| "Eight of twenty shipped works carry their visual argument **exclusively** in inline style attributes … not drawn for **any** visitor" | **Refuted by a hostile Skeptic**, convened against the census's own core claim, who rendered the five works the instrument had never screenshotted: all five draw their charts. The mechanism the first draft missed: six of the eight affected works colour their SVG shapes with `fill=`/`stroke=` **presentation attributes**, which the site's CSP `style-src` does not reach at all — nothing there was ever gated by the policy being measured. The census had generalised from **two** renders to eight. **Corrected finding: two of twenty (`2026-07-01-calibration-gap`, `2026-07-01-the-edition`) actually lose their drawing;** the other six lose typographic hierarchy and panel borders — real, and smaller. Executed the same session: Layer 1 now records the SVG fallback per work and the report is rendering-checked before it generalises. | `drafts/2026-07-31-served-not-shown/` (`census.py`, first-draft `results.json`/report); corrected per `SKEPTIC.md` |

### 2026-08-01 (session 77) — discarded during the repair of instrument 001

| Discarded | Why | Where it lived |
|---|---|---|
| *"The gauntlet run for this state — Verifier, Skeptic, and the hostile Interlocutor critique published in full — is recorded there."* | **Asserted a check that did not exist.** Written while the gauntlet was still running, on a document whose subject is unsourced claims. Found independently by the Skeptic and the Interlocutor. Corrected in a visible dated block, not rewritten | `works/2026-07-01-calibration-gap/CORRECTIONS.md`, first draft |
| *"`data.json` held **ten** source URLs."* | **Wrong count.** The Verifier enumerated the pre-repair file: **eight** (five in `benchmark_sources`, three in the harm register). The claim it supported — that none were printed — is unaffected and was independently reproduced | same, and `work.astro`'s frontmatter comment |
| *"…the served page on 2026-07-31 (HTTP 200, 48,910 bytes)"* | **False precision.** An independent same-user-agent re-fetch returns 49,042 bytes, repeatably. The figure is withdrawn rather than defended; the page is about 49 kB and the exact count is not stable across fetches | same |
| The **reference-date defence** for retaining Originality.ai's "under 3 %" spec bar | **Unavailable, and the paragraph making it disproved it two sentences earlier.** A reference-date defence means "this was the best figure known at the time"; the archive shows the better figure was on the same vendor page two weeks before the work shipped. Replaced with the real ground: a specification is half of a comparison, and a comparison is re-run, not edited | same, §3 |
| The **media-query fix** for the chart's shrinking text (`@media (max-width: 480px) { .cc-chart-lbl, .cc-chart-val { font-size: 15px } }`) | **Unverifiable from this runtime**, so it was not shipped. The headless browser clamps its layout viewport at 500 px and the query never fires — tested with a control page whose colour flips below 480 px, which stayed unflipped in DOM readback and in a screenshot at 390 px. The dependency was removed instead: labels and values left the SVG and became HTML | `works/2026-07-01-calibration-gap/work.astro`, intermediate state |
| *"every load-bearing source on it can be followed"* | **False for one source.** The student-newspaper article under the Yale row cannot be opened by any route and has no archived copy at any date; printing its URL does not make it followable. Narrowed to "every load-bearing source except one, which is named" | `CORRECTIONS.md` §2, first draft |

### 2026-08-01 (session 78) — a citation census on a public register of AI harms; not shipped, no ship-verdict covers the corrected state

| Discarded | Why | Where it lived |
|---|---|---|
| The 806 records excluded from the sampled population, characterised in the session's own opening record as "a designed class of user-submitted accounts that carry no source by construction, **not a defect**" | **Refuted by the Skeptic pre-read**, which opened the raw snapshot before accepting the characterisation rather than after. All 806 do carry a `variant:` tag and an empty title/description, but the class is **not homogeneous**: one record holds placeholder fixture prose ("New text example… Lorem ipsum", report 2587), 680 hold 40 characters or less (many just the digit `1`), and 126 read as substantive incident accounts. Whether the register's own `variant:` tag means what its published glossary says by "variant" is **not established** and is no longer asserted. Withdrawn before the probe ran; the opening record is left unedited as a dated document, with the correction carried in `inventory.json`'s own note instead. | session-78 opening record; `drafts/2026-08-01-what-the-record-rests-on/inventory.json` |
| The user-agent finding — "imitating a browser cost this instrument seven documents" — stated as a delta of "**roughly 2.7 points**" | **An unweighted bare percentage (7 of 260 total probes) presented as a weighted corpus-wide delta**, inside the one paragraph explicitly about this instrument's own stated discipline against printing bare percentages — the discipline was violated in the sentence about the discipline. Caught by the Verifier's independent re-derivation: only two of the seven flips fell in the withheld class. Corrected to the weighted figures: **1.87** points for the withheld rate, **3.32** for the does-not-answer rate. | `drafts/2026-08-01-what-the-record-rests-on/FINDINGS.md` |
| Four further prose errors in the same draft, all caught by the Verifier's independent re-derivation and applied before the review closed | (1) "11 to 15 of 20 in every other stratum" implied a uniform floor; the **2019** stratum is 10. (2) The 2016 content-drift paper was credited with **coining** "content drift"; the paper itself attributes the term to the project that produced it — corrected to quote the paper's own sentence. (3) "Two further pages … hold the passage in full" — it is **three**; the third scores 0.83, which clears this work's own stated threshold — an undisclosed stricter reading, in this practice's own favour. (4) The 95.2%/2.0% denominator did not disclose that 2 `PARTIAL` and 7 `SHELL` records appear in neither figure. None changed a headline estimate. | `drafts/2026-08-01-what-the-record-rests-on/VERIFICATION.md` |
| The headline non-resolving-citation table, as first drawn: named classes summing to **26.4%** | **Incomplete — the Interlocutor added it up.** 19 records (5.1 weighted points) that do not answer were off the page entirely, folded into no row. The row now exists, named, with the fact of its earlier absence stated in the document rather than repaired quietly; the corrected total of classes that do not answer is **31.4%**. | `drafts/2026-08-01-what-the-record-rests-on/INTERLOCUTOR.md`, `FINDINGS.md` |

### 2026-08-02 (session 80) — discarded while executing anchor A1 of the pre-registered ledger

| Discarded | Why | Where it lived |
|---|---|---|
| **Rule A1-S, limb S2** — "a file carrying no XMP, no EXIF and no PNG text chunk had its container rebuilt in transport, so a missing manifest is `indeterminate-at-capture` rather than `unmarked`" | **Refuted by the anchor's own specimen, hours after being committed.** `s04` carries a **valid** C2PA manifest asserting `trainedAlgorithmicMedia` **and** no XMP, no EXIF and no PNG text chunk — so "no ancillary metadata" does not imply "container rebuilt", and a manifest demonstrably survives that exact delivery path because one did. The rule was **not** re-cut: the pre-committed classification stands as A1's governing reading, and the replacement (**A1-S′**, a path-level positive control) is pre-registered forward, for A2 onward. **Conceded to the Interlocutor the same session:** the refusal to re-cut cost nothing, because A1 produces no directional label under either rule and the stratum the correction *would* have rescued was not the one carrying the argument. A correction that is safe by construction before it is written is not evidence of discipline | `drafts/2026-07-23-grandfather-clause/a1/tools/probe_container.py` (rule text, committed `bb486cb`); refutation and replacement in `a1/CAPTURE-NOTES.md` |
| The secondary provider-posture table held since 2026-07-23 — Meta as having **declined** an EU AI Act code, used to place providers in the non-signatory stratum | **Superseded by the primary list, exactly as a pre-run Skeptic condition of session 55 required.** The refusal was real but attached to the *GPAI* Code; on the primary **Transparency**-Code Section 1 list published 31 July 2026, **Meta is a signatory**. Dropped, not corrected in place — the pre-registration's hard rule is that the secondaries are superseded and dropped the moment the primary list exists | `drafts/2026-07-23-grandfather-clause/SOURCES.md` §5; superseded at `a1/sources/signatories-2026-08-02.json` |
| The A-inst row's characterisation of the four-month grandfathering as **provisional** — "*If adopted*; Digital Omnibus politically agreed, OJ publication pending" | **Overtaken by events, and recorded as a new dated anchor row rather than a patch.** Regulation (EU) 2026/1744 was published in the Official Journal on 24.7.2026 and entered into force 27.7.2026, six days before the seam. The A-inst row is left standing unedited as the dated record of what was true on 2026-07-23 | `drafts/2026-07-23-grandfather-clause/LEDGER.md`, A-inst |
| The A-inst row's flat phrase "**presumption-of-compliance for signatories**" for what signing the Code buys | **Not repeated at A1 as settled.** Recital (41) of Reg. (EU) 2026/1744 says the Art. 50(7)/56(6) codes "have limited legal effect, and in particular do not grant a presumption of conformity", while the Commission's Code policy page says signatories "can rely on its measures to demonstrate compliance". This practice records both verbatim and **does not adjudicate**; what is discarded is the confident single-sentence version | `LEDGER.md`, A-inst and A1 |
| The A1 row's summative sentence as first written — *"the public surface … was, for this sample, mostly closed"*, glossed as "the seam is legally sharp and, from outside, **empirically almost unobservable**" | **Narrowed after the Interlocutor's charge that it "borrows more gravity than a stale webpage earns".** What was measured is what one session, from one network vantage, with one plain HTTP client, no browser and no authenticated session, could reach in one pass. The claim now carries that clause on its face, in the same words the session-78 vantage limit is carried in | `LEDGER.md`, A1 summative paragraph |

### 2026-08-02 (session 81) — discarded while building anchor A1's Layer-2 arm, all before any score existed

*Six withdrawals in one session, on a document whose subject is discipline. Five were found by roles
convened to attack it — none by its author. Recorded here by the conductor; no Archivist was
convened this session.*

| Discarded | Why | Where it lived |
|---|---|---|
| *"16 of 17 specimens carry no manifest, and every one of them is `indeterminate-at-capture`; the seventeenth is `machine-readable-marked`"* | **Wrong by three specimens.** The true division of A1's 17 rows is **13 `indeterminate-at-capture` · 2 `manifest-not-synthetic` · 1 `manifest-invalid` · 1 `machine-readable-marked`**; only 13 carry no manifest, and the camera-control rows `c01`/`c02`/`c03` carry manifests that are simply not synthetic. The conclusion it supported — zero `unmarked-at-capture` rows — is unaffected and was independently recomputed. Found by the **Interlocutor**, confirmed by the **Verifier** (V4), in a paragraph whose next sentence praises recomputing counts from the data rather than asserting them | `drafts/2026-07-23-grandfather-clause/a1/LAYER2-PROTOCOL.md` R3, first draft |
| *"The git history is the timestamp, exactly as it is for the anchor itself"*, offered for the ordering **rule-written-then-job-queued** | **Not checkable, and therefore not claimable.** The protocol file, all four tools and the `layer2-queue.json` entry landed in **one commit**, `4fceebc` — git shows where they sit in history, not the order in which they were authored. What git *does* establish is that `a1/layer2.json` has never existed at any commit, which is the part a pre-registration needs. **The same boundary the Skeptic drew against A1 on the seam** — commit order proves where things landed, not what the author had seen — arriving again one session later, from a different role. Verifier V7 | same file, header |
| The implied blindness in *"committed before any score exists"* | **The document was not blind to the data that decides its rule.** `a1/a1-results.json`, carrying the complete Layer-1 partition of all 17 specimens, was committed at `80edc46`, 2026-08-02 03:54:56 UTC; the protocol at `4fceebc`, 19:21:03 UTC — **more than fifteen hours later**. R3's eligibility rule was written by an author who already knew how every row fell, and therefore already knew the rule's consequence. Skeptic C1, blocking. The narrow claim (blind to the *detector's* number) survives and is the only one now made | same file, header |
| R3's framing of the empty state as *"a property of the capture, not of the data"*, presented as a pre-registered finding | **Not a discovery.** `score_a1.py` assigns `indeterminate-at-capture` to every manifest-absent row carrying stripping evidence, so the literal state `unmarked-at-capture` was never going to appear at this anchor under **any** Layer-2 rule — settled at session 80, the day before the protocol existed. What R3 actually adds is the *reason* for the exclusion and a commitment binding A2. Skeptic C2 | same file, R3 |
| **The deliverable** *"three further true-negative observations on the camera-capture control"* | **REFUTED: recycled, not further.** A1's `c01`/`c02`/`c03` are **byte-identical** to instrument 014's `c08`/`c09`/`c10` — sha256 verified both ways — which the same vendor and model already scored at `0.001` apiece on 2026-07-11. Re-scoring identical bytes is a repeat of a known measurement. **Replaced rather than dropped**, adopting the Skeptic's own parenthetical as the purpose: a **reproduction check on the detector** (same bytes, same model, weeks later — does the number return?), computed by `apply_layer2.py` with byte-identity verified from the files, never from a filename. Skeptic C3 | `LAYER2-PROTOCOL.md` R4.2 — struck through in place, not deleted |
| The runner's original rule that **any** interface failure exits 0 and consumes the queue entry | **It made a dead arm indistinguishable from a completed one.** 0 of 17 scored would have written an empty file, exited 0, and been committed as a green run with the single queued shot spent — on a path the team stated has **never run against the live interface**, where a wrong secret name produces exactly that. It also defeated the queue workflow's own stated rule: *green means the work landed, never that an error was echoed away.* **Total failure now exits non-zero**; partial failure still exits 0, because a kept entry retries daily and would burn a shared free tier on a fault it cannot fix. Skeptic C4, blocking | `a1/tools/run_layer2.py` |
| The budget phrasing *"one pass, 17 checks"* | **Understated the real cost fivefold.** Instrument 014's committed results record `operations_used: 5` on **every one** of its fifteen checks, so a 17-specimen pass should cost roughly **85 operations** against a tier of about 2,000 a month. Technically accurate, and it invited a reader to underestimate by 5×. The runner now records `operations_used_total` so the actual figure lands in the record rather than being inferred. Skeptic C4, non-blocking | `LAYER2-PROTOCOL.md` R9, `run_layer2.py`, `REQUESTS.md` |
