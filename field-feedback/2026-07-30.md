# Build feedback 2026-07-30

Your contribution did not pass the build gate, and the failing files are yours. The errors are quoted below — please correct them and land again.

Run: https://github.com/frankbueltge/frankbueltge.de/actions/runs/30573756505

Failing lines, verbatim from the validation log:

```
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:208:372 - error ts(2304): Cannot find name 'v'.
208             <li>The snapshot's own asset list packages a harvest file for every run except the withheld source's two, though both of that source's manifests declare a file and a checksum: {(a12?.run_file_present_in_snapshot_assets_by_source as any) && Object.entries(a12!.run_file_present_in_snapshot_assets_by_source as Record<string, boolean>).map(([k, v]) => `${k}: ${v ? 'present' : 'absent'}`).join(', ')} <span class="ref">[A12]</span>.</li>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:208:366 - error ts(2304): Cannot find name 'k'.
208             <li>The snapshot's own asset list packages a harvest file for every run except the withheld source's two, though both of that source's manifests declare a file and a checksum: {(a12?.run_file_present_in_snapshot_assets_by_source as any) && Object.entries(a12!.run_file_present_in_snapshot_assets_by_source as Record<string, boolean>).map(([k, v]) => `${k}: ${v ? 'present' : 'absent'}`).join(', ')} <span class="ref">[A12]</span>.</li>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:208:356 - error ts(2304): Cannot find name 'v'.
208             <li>The snapshot's own asset list packages a harvest file for every run except the withheld source's two, though both of that source's manifests declare a file and a checksum: {(a12?.run_file_present_in_snapshot_assets_by_source as any) && Object.entries(a12!.run_file_present_in_snapshot_assets_by_source as Record<string, boolean>).map(([k, v]) => `${k}: ${v ? 'present' : 'absent'}`).join(', ')} <span class="ref">[A12]</span>.</li>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:208:353 - error ts(2304): Cannot find name 'k'.
208             <li>The snapshot's own asset list packages a harvest file for every run except the withheld source's two, though both of that source's manifests declare a file and a checksum: {(a12?.run_file_present_in_snapshot_assets_by_source as any) && Object.entries(a12!.run_file_present_in_snapshot_assets_by_source as Record<string, boolean>).map(([k, v]) => `${k}: ${v ? 'present' : 'absent'}`).join(', ')} <span class="ref">[A12]</span>.</li>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:208:351 - error ts(2364): The left-hand side of an assignment expression must be a variable or a property access.
208             <li>The snapshot's own asset list packages a harvest file for every run except the withheld source's two, though both of that source's manifests declare a file and a checksum: {(a12?.run_file_present_in_snapshot_assets_by_source as any) && Object.entries(a12!.run_file_present_in_snapshot_assets_by_source as Record<string, boolean>).map(([k, v]) => `${k}: ${v ? 'present' : 'absent'}`).join(', ')} <span class="ref">[A12]</span>.</li>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:208:337 - error ts(2554): Expected 1 arguments, but got 2.
208             <li>The snapshot's own asset list packages a harvest file for every run except the withheld source's two, though both of that source's manifests declare a file and a checksum: {(a12?.run_file_present_in_snapshot_assets_by_source as any) && Object.entries(a12!.run_file_present_in_snapshot_assets_by_source as Record<string, boolean>).map(([k, v]) => `${k}: ${v ? 'present' : 'absent'}`).join(', ')} <span class="ref">[A12]</span>.</li>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:208:337 - error ts(2693): 'boolean' only refers to a type, but is being used as a value here.
208             <li>The snapshot's own asset list packages a harvest file for every run except the withheld source's two, though both of that source's manifests declare a file and a checksum: {(a12?.run_file_present_in_snapshot_assets_by_source as any) && Object.entries(a12!.run_file_present_in_snapshot_assets_by_source as Record<string, boolean>).map(([k, v]) => `${k}: ${v ? 'present' : 'absent'}`).join(', ')} <span class="ref">[A12]</span>.</li>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:208:329 - error ts(2693): 'string' only refers to a type, but is being used as a value here.
208             <li>The snapshot's own asset list packages a harvest file for every run except the withheld source's two, though both of that source's manifests declare a file and a checksum: {(a12?.run_file_present_in_snapshot_assets_by_source as any) && Object.entries(a12!.run_file_present_in_snapshot_assets_by_source as Record<string, boolean>).map(([k, v]) => `${k}: ${v ? 'present' : 'absent'}`).join(', ')} <span class="ref">[A12]</span>.</li>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:208:322 - error ts(2314): Generic type 'Record' requires 2 type argument(s).
208             <li>The snapshot's own asset list packages a harvest file for every run except the withheld source's two, though both of that source's manifests declare a file and a checksum: {(a12?.run_file_present_in_snapshot_assets_by_source as any) && Object.entries(a12!.run_file_present_in_snapshot_assets_by_source as Record<string, boolean>).map(([k, v]) => `${k}: ${v ? 'present' : 'absent'}`).join(', ')} <span class="ref">[A12]</span>.</li>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:546:1 - error ts(1128): Declaration or statement expected.
546 </div>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:498:3 - error ts(2657): JSX expressions must have one parent element.
498   <section class="assertions">
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:418:5 - error ts(17002): Expected corresponding JSX closing tag for 'Fragment'.
418   </section>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:223:9 - error ts(17002): Expected corresponding JSX closing tag for 'article'.
223       </div>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:209:13 - error ts(17002): Expected corresponding JSX closing tag for 'div'.
209           </ul>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:208:443 - error ts(1381): Unexpected token. Did you mean `{'}'}` or `&rbrace;`?
208             <li>The snapshot's own asset list packages a harvest file for every run except the withheld source's two, though both of that source's manifests declare a file and a checksum: {(a12?.run_file_present_in_snapshot_assets_by_source as any) && Object.entries(a12!.run_file_present_in_snapshot_assets_by_source as Record<string, boolean>).map(([k, v]) => `${k}: ${v ? 'present' : 'absent'}`).join(', ')} <span class="ref">[A12]</span>.</li>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:208:345 - error ts(1109): Expression expected.
208             <li>The snapshot's own asset list packages a harvest file for every run except the withheld source's two, though both of that source's manifests declare a file and a checksum: {(a12?.run_file_present_in_snapshot_assets_by_source as any) && Object.entries(a12!.run_file_present_in_snapshot_assets_by_source as Record<string, boolean>).map(([k, v]) => `${k}: ${v ? 'present' : 'absent'}`).join(', ')} <span class="ref">[A12]</span>.</li>
src/components/field/werke/2026-07-26-one-line-for-ten-thousand/index.astro:196:4 - error ts(17008): HTML element 'section' has no corresponding closing tag.
196   <section class="findings">
```

No deploy happened; the last good state stays live.
