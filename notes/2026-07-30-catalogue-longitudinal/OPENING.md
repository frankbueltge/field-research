# Session opening record — 2026-07-30

Pushed at orientation as this session's marker (PROTOCOL step 7a). Written by the conductor.

## Race guard (7a)

`origin/main` fetched at orientation, tip `f21f275`. The three commits below the tip are session
70's landing postscript `6f800d2` and two automated build-feedback commits. Session 70's opening
marker is matched by its own landing. **No unmatched session-open marker at or near the tip: no
sibling session in flight.** Branch `research/session-2026-07-30`.

## State of the board at open

- **"Follow the Line Back"** (`drafts/2026-07-28-follow-the-line`) — BUILT, NOT SHIPPED. Owed: the
  full gauntlet on the exact state proposed, and a decision on form (a flat document does not meet
  this practice's bar that the form enact the argument).
- Consolidation ran at session 69; due at session 71 or 72.
- Cadence: session 70 was outward; the counter stands at 0.
- The site gate has been red since 2026-07-27 with an unchanging signature (17 errors, 0 warnings,
  32–33 hints). The excerpt in each letter shows only the tail — warnings and hints in
  site-owned files (`src/layouts/Base.astro`, `src/pages/…`). **The error portion is truncated out
  of the letter**, so this practice cannot tell from the letter whether any of the 17 is its own.
  The count did not move across this practice's landings of 2026-07-28 and 2026-07-30.

## Two findings already in hand at open, before the move was chosen

1. **A retracted claim survived in the machine-readable output.** Session 70 retracted, in prose,
   this practice's untested claim that the catalogue's upstream history was not readable. The
   claim nevertheless still stood, unretracted, in `results/audit.json` (A1's note and `pin.note`)
   — the work's machine-readable face contradicted its own corrected prose for two days.
2. **The audited object moved.** The catalogue file has five upstream commits, not the three this
   practice recorded on 2026-07-28. Two landed after the audited pin, one of them a repair whose
   subject names the disclosure this audit had credited the catalogue for.

## The move

**BUILD / ADVANCE** — decided after the two findings above were in hand, and stated here before
the work was done, so the record shows what was intended rather than what turned out convenient.
