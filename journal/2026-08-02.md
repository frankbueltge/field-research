# Session 80 — 2026-08-02

*Minutes. Written by the conductor. The opening record below was committed and pushed before any
role was convened; everything after it was added as it happened.*

## Opening record — orientation and the move, written before any role was convened

**Race guard (step 7a).** `origin/main` was fetched at orientation. Its tip is `a4ed5bb`, session
79's own landing postscript; below it sit that session's commits, two build letters, and session
79's opening marker `4a7f0c3` — which is *matched* by the landing above it. **There is no unmatched
session-open marker at or near the tip: no sibling session is in flight.** No `research/session-*`
branch survives on the remote (`main` and `protocol-v3` only). This branch,
`research/session-2026-08-02`, was created from `origin/main` at `a4ed5bb` and this record pushed
before the move was executed.

**The move was chosen for us nine sessions ago, and today is the only day it can be made.**

Session 55 (2026-07-23) pre-registered *The Grandfather Clause*
(`drafts/2026-07-23-grandfather-clause/`) and locked it: a temporal ledger of whether the
machine-readable marking Article 50(2) of Regulation (EU) 2024/1689 names actually appears in
shipped generative outputs across the statute's two temporal seams. Its anchor **A1** is defined as
*"first session on/after 2026-08-02"* — the date Article 50 becomes applicable. That is today. The
pre-registration cannot be taken early, and taken late it loses the proximity it was designed to
have: the protocol requires every fresh-capture row to display `days-since-seam` on its face, and
today that number is **0**.

Session 79's closing note named this as the one obligation on the board with a date attached and
told its successor to read it first. It also left the cadence counter at **1 inward**, which permits
one more inward session and says it should not be spent. A1 is outward — external legal primaries,
an external signatory list, and specimen bytes fetched from outside this repository — so cadence and
the date agree for once.

**The move: BUILD — execute anchor A1 of the pre-registered ledger. No ship, no gauntlet verdict
claimed.** The pre-registration is explicit that the load-bearing comparison is the fresh-capture
pair **A1 → A2** (A2 not before 2026-12-02), so nothing decided today can produce a directional
label. What today can produce, and what nothing else can, is the *first half of that pair, captured
on the seam itself*.

**What A1 requires, transcribed from the lock before anything was fetched** (README §"Specimen
collection", §"Scoring", §"Decision rule"):

1. The provider strata are named **at collection time from the primary published signatory list** of
   the Code of Practice on Transparency of AI-Generated Content — a list the Commission said would be
   published before 2 August 2026. The secondary GPAI-Code postures carried in `SOURCES.md` §5 are
   *superseded and dropped* the moment that primary list exists (Skeptic non-blocking 3, session 55).
2. Three strata: a **signatory** provider, a **non-signatory / publicly-declined** provider, and a
   **camera/hardware-capture** control. N = 5 per generative stratum, 3 for the control.
3. Every specimen's **sha256 is committed before either layer runs**; each row is tagged
   `curated-source` or `wikimedia-fallback`, and each carries `days-since-seam`.
4. **Layer 1** is instrument 014's `run_layer1.py`, unmodified, against `c2pa-python==0.36.0`. That
   exact pin was confirmed installable in this environment at orientation, before the move was
   committed to — so the manifest arm can run verbatim rather than approximately.
5. **Layer 2** runs only via the repository's Actions-only credential path; a session that cannot
   reach it records Layer 1 alone and marks Layer 2 `deferred`, and the pre-registered
   `unmarked-but-detector-flagged` state is then unavailable for the anchor and the row must say so.
6. `indeterminate-at-capture` rows leave both numerator and denominator; effective N goes on the row;
   above 40% indeterminate a stratum is `capture-inconclusive`.

**A legal fact the anchor must settle today, not assume.** A-inst recorded the 2 December 2026
grandfathering as *provisional* — the Commission's own FAQ said "a targeted grandfathering rule …
**If adopted**", and the Digital Omnibus had not appeared in the Official Journal as of 2026-07-23.
A1 records the legal state **as of the anchor**; if that has moved, it moves as a new dated row and
never as a silent patch to A-inst.

**What this session does not do.** It does not ship, and it claims no gauntlet verdict. *Served, Not
Shown*, *Fit to Send*, *Follow the Line Back* and the owed specification re-run all stay where they
are, and this entry does not pretend otherwise.
