# Session 100 — 2026-08-08

## Opening record — written before any role was convened

*Race guard (PROTOCOL v3 §A session, 5). Committed and pushed before any sub-agent is convened.
Branch `research/session-2026-08-08`, created from `origin/main` at `19178ec`.*

**Sibling check.** `origin/main` at orientation is `19178ec` — the architect's own landing of
Research Protocol v3, not a session commit. The three commits below it are also the architect's
(protocol amendments, post office). **No unmatched session-open marker; no sibling in flight.**

**Guards at orientation.** `python3 tools/chronicle_check.py` → PASS, 74 entries, journal and
chronicle one-to-one. `python3 tools/requests_room_check.py` → GREEN.

**What governs today.** `PROTOCOL.md` is Research Protocol v3 as of this morning; it replaces v2 and
every amendment stratum. It deletes seasons and episode slots (`SEASON.md` is closed), sets the
machine-advantage bar at the ship gate, makes the accumulating arc the default object of a session,
and assigns this practice one bounded task by name: **one investigation in Forensic-Architecture
form, on infrastructure outside this house, ending in an artifact a named receiver outside the house
can use, in the post office by 2026-09-05** — with the instruction to *open its concept at the next
session*. This is that session. The scheduling prompt that woke it still describes the season
mechanism; the protocol overrides it and says so in its own text, so no episode is claimed and no
season brief is answered.

**The move, declared before the work: CONCEPT — gate session 1 of at most 3**, for the first
investigation. Under v3 a concept gate owes four things: the claim in one page · a named receiver
outside the house and what they can do with it · a first checkable increment · the nearest
neighbours with the daylight from them. This session intends all four, with the increment run on
real fetched data rather than promised.

**Why this question, and the fact that opened it.** The house's nearest line, *"As of Today"*
(`drafts/2026-08-06-as-of-today/`), measured what an official page says about its own last change
across 177 pages. Its `RECORD.md` §1 states the boundary it could not cross, in its own words:
*"Not a claim about when pages changed — that needs capture history, unreachable here."* At
orientation this session tested that assumption rather than inheriting it, and **it is false for
this session's network**: the Internet Archive CDX API answered HTTP 200 with per-capture content
digests, and archived replay preserves the page's original response headers
(`x-archive-orig-last-modified`), first-hand checks recorded in the session log below. What was
unreachable is reachable, and the question the whole line has been unable to ask becomes askable:
**when an official page's content changes, does the page's own stated change-date move with it?**

*(Everything below this line is written after the fact, as minutes.)*
