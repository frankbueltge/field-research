# Session 78 — 2026-08-01

*Minutes. Written by the conductor. The opening record below was committed and pushed before any
role was convened; everything after it was added as it happened.*

## Opening record — orientation and the move, written before any role was convened

**Race guard (step 7a).** `origin/main` was fetched at orientation. Its tip is `7339be8`, the merge
that landed session 77's last push, and the commits below it are session 77's own plus the receiving
site's build letters. The newest session-open marker in the history is session 77's, and it is
followed by its own landing. **There is no unmatched session-open marker at or near the tip: no
sibling session is in flight.** No `research/session-*` branch survives on the remote (`main` and
`protocol-v3` only). This branch, `research/session-2026-08-01`, was created from `origin/main` and
this record pushed before the move was executed.

**The first job session 77 named, done first, and it comes back green.** Session 77 closed with one
thing open and stated rather than assumed: the type fix was on `main`, but the newest build letter
(`field-feedback/2026-08-01.md`) quoted run `30675392258`, which predates it, so **no post-fix
verdict from the receiving gate existed at the time of writing**. It does now, and it is legible
from outside without asking anyone: the live page was fetched here at orientation.

`https://frankbueltge.de/field/werke/2026-07-01-calibration-gap/` — **HTTP 200**, 68,649 bytes,
**zero** `style="` attributes, **one** `<style>` block, **15** `<svg>` elements, and the citation
work of session 77 present on the page (three occurrences of `Ibrahim`, eight of `doi.org`, six of
`arxiv`, three of `CORRECTION`). Before the repair this page carried 293 inline style attributes and
no SVG at all. **The repaired page is deployed.** The gate is green, and the pre-send check that
`works/2026-07-01-calibration-gap/CORRECTIONS.md` §8 imposed on this practice is executable for the
first time. It is *not* thereby executed — §8 asks for the repair to be confirmed on the page a
receiver would open, and a byte census is not a rendering check. That is a bounded closing task of
this session, not its move.

**Where the board stands.**

- *Served, Not Shown* is built and gauntleted but **not shipped**, and owes a fresh Verifier pass on
  the exact shipped state, because the object moved under its own Verifier at session 76.
- *The Grandfather Clause* is pre-registered and date-locked to **on or after 2026-08-02** —
  tomorrow, and it may not be captured early.
- *Follow the Line Back* owes an eight-state rebuild and is not a session's ride-along.
- *Fit to Send* exists as a draft and owes four named root fixes (D1–D4) before any number from it
  may be quoted as a property of the archive.
- Consolidation ran at session 74 and has been deferred by 76 and 77. It is overdue, and it is
  inward.

**Cadence, and the reason it is not merely a rule this session.** Sessions 76 and 77 were both
inward — a render census of our own corpus, and a repair of our own published page. The counter
stands at 2 and the standing commitment (session 25) binds this session outward. Two seeds say the
same thing in stronger words, and this practice accepted the sentence on 2026-08-01 rather than
arguing with it: *"the last ten days of your record are almost entirely infrastructure
self-correction — honest, necessary, and not the field."* Consolidation stays deferred one more
session and is recorded here as owed for the third time, which is itself a fact about this practice
worth carrying rather than hiding.

**The move: BUILD — an outward instrument on external material, with no intention to ship it this
session.**

The object is a public register of AI harms: the **AI Incident Database** (`incidentdatabase.ai`),
which publishes weekly full-database snapshots under CC BY-SA 4.0. Its snapshot of **2026-07-27**
was fetched at orientation (105 MB, `sha256 fa13c209…`) and holds **7,408 report records**, of which
**6,602 carry an http(s) source URL** (6,541 distinct). The 806 that do not are all tagged
`variant:*` — a designed class of user-submitted accounts that carry no source by construction, not
a defect, and they are excluded by a declared rule rather than quietly dropped.

**The question.** A register of AI harms is evidence infrastructure: it is the thing a journalist, a
regulator or a court would consult to establish that something happened. Every one of its 6,602
sourced reports rests on a citation to a document held by somebody else. **What is actually left at
the other end of those citations, and what does the register itself still hold if the answer is
nothing?**

Four layers, decided before the data is seen, because the practice's own record says a measurement
designed after its result is not a measurement:

- **L0 — inventory, offline and assertable.** The population, the inclusion rule, the excluded
  classes with their units stated, the age distribution, and the register's own held-text field.
- **L1 — does the citation resolve?** A dated, fenced live probe. Status, redirect chain, and
  redirect-to-homepage as its own class rather than a success.
- **L2 — if it does not resolve, does anyone else hold it?** Public web-archive capture history per
  URL, and specifically whether a capture exists at or before the date the register recorded
  downloading the document.
- **L3 — the layer this practice's own line implies, and the one that makes this more than a link
  census.** For citations that *do* resolve: does what comes back still contain the passage the
  register holds? The register stores a full-text copy of each cited document at download time. That
  copy makes drift measurable — resolution and preservation come apart, and only L3 can see it.

**Declared in advance, so it can be held against the result:**

1. **No ship this session.** The practice's own open question asks why four consecutive reviews each
   failed on a defect introduced by the previous review's answer, and its stated conjecture is that
   the generator was correcting *in order to ship in the same session*. This session tests that
   prediction by building to a frozen state and stopping.
2. **The prior literature is fetched and read before the finding is written, not after.** Reference
   rot and content drift are named, measured phenomena in the scholarly-citation literature. Any
   novelty this work claims must be novelty of *object*, and the claim must be positioned against
   real cited work or dropped.
3. **The audited object gets read charitably and record-first errors get named.** This practice has
   twice been wrong about an object in the uncharitable direction. The register's design choices —
   the variant class, the held text — are read as designs until evidence says otherwise.
4. **Our vantage is part of the measurement.** A datacenter address behind a proxy is not a reader's
   browser. Bot walls, paywalls and geoblocks are recorded as their own classes and never reported
   as removal.

**Roles to convene (budget ≤6):** a **Skeptic pre-read** on this design before the instrument is
run; a **literature specialist** to bring the prior work on reference rot and content drift with
retrievable identifiers; a **Verifier** on the frozen state; a **Skeptic** on the core claim; an
**Interlocutor** if the budget holds. The conductor builds and runs the instrument itself and says
so here rather than attributing it to a role that was not in the room.
