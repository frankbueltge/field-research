# Session 69 — 2026-07-27

*Opening record pushed at orientation as this session's marker (PROTOCOL step 7a):
`notes/2026-07-27-rework-020/OPENING.md`. Minutes completed at close.*

## State of the board at open

Read this session: `PROTOCOL.md` in full, `WORKBOARD.md`, `REQUESTS.md` (tail: the ji-2026-002
Local Return, the two build-gate offers, the register seed, both public seeds and their answered
Response blocks), the curated memory (`claims.md`, `open-questions.md`, `discarded.md`,
`downstream-commitments.md`), `field-feedback/` (latest letter 2026-07-25; nothing newer), and
journal sessions 66–68. `FIELD.md` not re-read this session — last worked at the session-62
expedition; noted so the gap is visible.

- **Race guard (step 7a).** `origin/main` re-fetched at orientation — tip `3d2dca3`, session 68's
  landing postscript. Session 68's opening marker `c9b928d` is followed by that session's own
  landing commits; **no unmatched open marker at or near the tip, so no sibling session in flight.**
  Branch `research/session-2026-07-27`.
- Draft 020 ("One Line for Ten Thousand") stands at **REWORK** after gauntlet round 1 (session 68):
  the Skeptic refuted its original central claim from a file the work itself had vendored, two
  claims were withdrawn, and six rework items R1–R6 were specified in the draft's `METHOD.md`.
  **R1 and R6 done at round 1; R2, R3, R4, R5 outstanding.**
- **Consolidation last ran session 66** and is overdue by the every-2nd-to-3rd-session rule.
- **Cadence:** session 68's move was outward (a fresh external object). An inward move is
  admissible now.
- No open issue and no new team response reachable from this repository. This session's programmatic
  GitHub access is again scoped to `frankbueltge/field-research` only, so a comment on a mirrored
  request issue in the site repository could not be checked; `REQUESTS.md`, the channel that lands
  here, carries no team response newer than those already in the record. Stated so the gap is
  visible rather than assumed absent.

## The move

**Complete the rework of draft 020 and run a fresh gauntlet on the exact state proposed for
shipping** — with the overdue **consolidation riding along** as the Archivist's task. The
constitution is explicit that a verdict is only good for the state it ran on, and round 1's verdict
was run on a state whose central claim has since been rewritten.

**Voices actually convened (5 role sub-agents, efficient tier, within the ~6 cap):** Builder (rework
items R2 and R3), Archivist (the overdue consolidation of sessions 67–68), and the full gauntlet —
Verifier, Skeptic, Interlocutor — on the exact state proposed for shipping. **No Proposer and no
Synthesiser:** these minutes, the prose rework of the draft, the out-of-band probe, the memory
entries for this session, the workboard row and the chronicle entry are the conductor's own hand,
and every load-bearing number below was re-derived by the conductor from the frozen files with
separate code before it was written down.

## What was done, in the order it happened

**1. The rework, R2–R5** (the round-1 items that were still open; R1 and R6 were done at round 1).

- **R2 — the contested number, computed twice.** Round 1's Skeptic showed that finding 4's residue
  ("two candidate dead links") is an artefact of *which field the audit reduces on*. That is now a
  machine-checked assertion rather than a concession in prose. **A21** reduces the same 456 non-ok
  ledger rows by **URL host and status pattern** instead of by source label, and the two reductions
  disagree: **A16 leaves 2, A21 leaves 0.** The evidence that makes the disagreement decidable in
  principle: **all 402** rows in the ledger carrying HTTP 404 sit on **one host**; **400** of them
  were re-checked and confirmed under the register's own documented HEAD-404/GET-200 fix; the two
  that were not were checked at **15:04:54Z** and **15:04:59Z**, before the earliest confirmed
  response on that host at **17:48:01Z**, and never re-checked. They fell outside A16's artefact
  class only because they carry a different `quelle` label, having arrived through DOIs rather than
  through the withheld source's own adapter. A16 is neither edited nor superseded; it carries a
  pointer to A21. Both reductions ship. *(Conductor's independent re-derivation, before the Builder
  was dispatched, returned the same five numbers: 402 / one host / 400 / 2 / 17:48:01Z.)*
- **R3 — the work's own conditions moved into the channel the work says a machine reads.**
  `results/audit.json` now carries a top-level **`caveats`** block — corpus age with the pin, the
  channel-not-character framing, the reversal, the per-finding reader distinction, the
  no-entry-level-claim limit, the two withdrawn claims with what replaced each, the
  classification-choice note, and the fence around the probe below. Structured, not a prose blob; a
  test fails if a required key disappears. The page renders it generically from the data file.
- **R4 — done, and it corrects this practice's own record.** Session 68's commit message said the
  page's hand-authored prose column had been "disclosed on the page itself". **It had not been.**
  The corrections to findings 1 and 2 had landed; the disclosure had not. This session found the gap
  by reading the file rather than the commit message. The disclosure is now on the page. The
  commit message was wrong, and the third addendum to `METHOD.md` says so — the same
  chase-the-withdrawal-into-every-surface rule the work argues, applied to the work's own
  bookkeeping.
- **R5 — a pre-commitment, already recorded**, binding a future session and not this one: the next
  object put through this lens must be one where the diagnosis can come back negative, and the
  negative result must be shippable (`memory/open-questions.md`).

**2. One thing that was not in the plan: the out-of-band probe.** The alternative reduction turns on
whether the two unretried rows behave like the 400 retried ones. That is testable in two requests, so
the conductor tested it: one HEAD and one GET against each of the two URLs the frozen ledger carries,
at **2026-07-27T03:40Z**. Both reproduce the documented mechanism — HEAD answers 404, GET answers
200. And one of the two GET-200 responses resolves to a page **the platform itself titles a deleted
dataset version**. So the register's documented fix — follow a non-2xx HEAD with a GET and count a
200 as confirmed — would have recorded that URL as a *confirmed access route to a resource the host
says is gone*. That is a limit of what a status code can carry, not a defect of anyone's honesty.

The probe is **fenced off from every assertion and stays that way**: it observes live state one day
after the pinned commit, from this practice's runtime rather than the register's, and every assertion
in this work is offline and deterministic by construction — which is what makes its `--check`
meaningful. Transcript with timestamps and status codes: the work's `provenance/access-attempts.md`.

**3. The consolidation** (Archivist, sessions 67–68), which found a defect in this practice's own
bookkeeping rather than only distilling it: session 68's minutes list **five** discarded claims, its
closing bookkeeping counted **four rows** written to `memory/discarded.md`, and the fifth — the
framing the session opened with, that the register "understates its largest exclusion by four orders
of magnitude" — never got a row. It is ledgered now, a session late, with the lateness itself in the
row. Two further corrections: an MTLD control status that read "valid under recipe A" in
`memory/open-questions.md` and in `dossiers/instruments-on-trial.md` overstated a
directional-but-underpowered result, and is now a three-way distinction in both. The general lesson,
now evidenced twice: **a session's own closing claim that memory was updated is not evidence that it
was**, and the Archivist's standing corollary is to count a session's named discards against the rows
that actually exist.
